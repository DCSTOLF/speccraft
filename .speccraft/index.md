# speccraft

A Claude Code plugin that enforces spec-first TDD via hooks, slash commands, subagents, and cross-model review.

## Stack

- Bash 5+ hooks (`hooks/`) wired through `hooks/hooks.json`
- Go helper binaries under `tools/cmd/speccraft-{state,guard,drift}` sharing `tools/internal/{speccraft,delegate}` (module `github.com/dcstolf/speccraft/tools`; `go.mod` declares Go 1.22, CI runs Go 1.26.3)
- Markdown slash commands (`commands/`) and subagents (`agents/`)
- Markdown skills (`skills/<name>/SKILL.md`)
- Stack-agnostic memory templates (`templates/speccraft/`) copied into a host repo by `/speccraft:init`
- Devcontainer-based end-to-end test (`tests/e2e/run.sh`) driven by GitHub Actions (`.github/workflows/ci.yml`)

## Architecture in one paragraph

speccraft is packaged as a Claude Code plugin (`.claude-plugin/plugin.json`, marketplace `dcstolf-tools`) and ships three execution surfaces: shell hooks that gate Edit/Write tool calls, slash commands the user invokes (`/speccraft:init`, `/speccraft:spec:*`, `/speccraft:sync`), and subagents the orchestrator dispatches (planner, critic, reviewer, delegator, memory-keeper). Hooks and commands call small Go binaries — `speccraft-state` (session/spec state in `.speccraft/state.json`), `speccraft-guard` (TDD red→green invariant), and `speccraft-drift` (regex scan of `enforce:` rules in memory files) — whose shared logic lives in `tools/internal/speccraft`. The repo dogfoods its own plugin: `.speccraft/` here is real project memory for this very codebase, not a fixture.

## Hard rules (see guardrails.md)

- Never commit built binaries from `bin/` or `tools/bin/`.
- Never bypass the TDD red→green invariant without `/speccraft:spec:override` with a recorded reason.
- Plugin templates under `templates/speccraft/` must stay stack-agnostic (no Go-, Python-, or HTTP-specific assumptions).

## Where to look

- Hooks: `hooks/` (entry: `hooks/hooks.json`)
- Slash commands: `commands/` (top-level + `commands/spec/`)
- Subagents: `agents/`
- Skills: `skills/<name>/SKILL.md`
- Go helper binaries: `tools/cmd/speccraft-*/main.go`
- Shared Go logic: `tools/internal/speccraft/`, `tools/internal/delegate/`
- User-facing memory templates: `templates/speccraft/`
- E2E test harness: `tests/e2e/run.sh`
- Specs: `specs/NNNN-<slug>/`

## Active spec

_none_

## Recent decisions (last 3)

- 2026-07-26 — Diff-focused re-review scopes reviewers to the deltas; version 1.10.0 (spec 0035): `/speccraft:spec:review --diff` stops re-litigating already-settled sections on a re-run. The anchor is a review-TIME snapshot (`specs/<id>/review-snapshot.md`), NOT git history (review→revise→re-review precede the first commit). DETECTION lives in `speccraft-state`; the UX/provenance gate in the command. New Go (`tools/internal/speccraft/review.go`+`review_sections.go`): `AtomicWriteFile` (shared same-dir-temp+rename durable-write seam, injectable `atomicRename` for fault-injection); `WriteReviewSnapshot` (sha256 of RAW spec bytes, true no-op — no rewrite/mtime touch — when byte-identical); `ReviewDiff` returning `ReviewEnvelope{schema:1,snapshot,changed,changed_sections,diff,fingerprint,base_fingerprint}` (reads spec.md ONCE; `--promote` freezes the SAME bytes AFTER computing the envelope = read-before-overwrite, AC11); `diffSections` determinism engine (structured `{kind,heading,ordinal,side}` anchors, pure ordinal-key matching, byte-identical bodies NEVER emitted, rename=removed+added, frontmatter/preamble reserved kinds); `WriteReviewFile` (AC2 single atomic commit — one `reviewed_sha256:` line, no intermediate fingerprint-free write). Three new `speccraft-state` subcommands via the `run()` seam: `review-snapshot write`, `review-diff [--promote]` (exit-code matrix; `--promote` refused on closed spec via `specStatusIsClosed`), and the UNPLANNED `review-commit` (CLI-boundary atomic commit). New `templates/prompts/re-review.md` (AC7a) + `commands/spec/review.lib.sh` (`review_reviewed_sha256` anchored parser, `review_classify` provenance gate, `review_build_payload` sourcing the FROZEN snapshot never spec.md). `review.md` wires the `--diff` flow. `speccraft-drift` `CheckFile` now excludes `specs/**` (AC10). Version 1.9.0→1.10.0 (three consts each a renamed sibling test + both manifests grep oracle). The load-bearing pins came from a 9-round cross-model review that drove out 4 real bugs pre-implementation: baseline-provenance (prior `reviewed_sha256` must == envelope `base_fingerprint`, else full review — a promoted-but-unreviewed baseline can't smuggle a delta past review), retry-safety (any `changed:false` lacking a matching review falls back to full), AC2-as-single-atomic-commit, and pure-ordinal-key determinism (replacing a blanket over-report). Deviations: ONE planned override (T1 new-symbol bootstrap, AC13 budget≤1 held); implemented in-place in review.go to dodge duplicate-symbol deadlocks under the stub guard; optional T3 `saveStateLocked` refactor skipped; incidental USER-made agents.toml codex `--full-auto`→`--sandbox workspace-write`. Recurring stale-1.1.0-cached-guard friction (REDs via Edit, fresh test-func as last sibling touch). Own close ran no consolidation.
- 2026-07-25 — MultiEdit/NotebookEdit payload modeling closes spec 0031's reserved slot, override-free; version 1.9.0 (spec 0032): `speccraft-guard`'s `applyEdit` modeled only Write (→`content`) and Edit (→in-place replace) after spec 0031; MultiEdit/NotebookEdit fell to a `default:` fallback (pre-edit content unchanged + a "reserved for spec 0032" comment + two characterization tests), so a test file authored via either tool captured ZERO red-candidates and the sibling prod edit was wrongly blocked — the class 0031 fixed for Write. Now modeled: NEW named type `MultiEditEntry{OldString,NewString}`; NEW `ToolInput` fields `Edits []MultiEditEntry` (json:"edits") + `NewSource string` (json:"new_source"); NEW helper `applyMultiEdit(pre,edits)` folding each entry as a FIRST-occurrence `strings.Replace` over the RUNNING content (later entry sees earlier output), SKIPPING an entry whose `OldString==""` (Go's Replace would prepend), no-op on absent target (best-effort; not the real tool's hard error). `applyEdit` gains `case "MultiEdit"` (→applyMultiEdit) + `case "NotebookEdit"` (→ti.NewSource, empty INCLUDED, mirroring Write's Content); switch keys on ToolName so the empty-vs-absent new_source zero-value ambiguity is moot (plain string, not *string, deliberate). NotebookEdit models the cell's new_source only — `.ipynb` JSON not parsed, other cells' test IDs unobserved (accepted, pinned). The two 0031 characterization tests INVERTED+RENAMED (`…CapturesNoRedCandidates`→`…CapturesRedCandidate`); NEW `reserved_slot_test.go` adds a case-insensitive/whitespace-tolerant recurrence grep (needles from concatenated fragments, self-excluded) forbidding surviving "reserved spec 0032"/"unmodeled" language + a `FutureEdit` fallback pin + two dispatch-injection PINs (AC8 regression, injection already existed) + `Test_ApplyEditDefaultComment_OmitsModeledTools`. Version 1.8.0→1.9.0 across three `const version` binaries (each via a renamed sibling version test) + both manifests (grep oracle); AC10's published-verified-release is the merge-time obligation (auto-tag→release.yml→verify-release.sh). META-PAYOFF: shipped OVERRIDE-FREE — every RED touching the new fields authored at the `json.Unmarshal` envelope boundary (unknown key→zero value→compiles vs old struct, fails on behavior), confirming 0031's technique GENERALIZES from one `Content` field to a struct-FIELD extension. Two findings: (1) stale 1.1.0 cached guard on PATH — a no-new-test Edit (adding an import) to `reserved_slot_test.go` CLEARED the standing sibling RED (SetRedCandidates replaces per-file), blocking the gated `default:`-comment scrub; remedied by registering a GENUINE fresh companion RED via Edit (keep the decisive fresh RED as the LAST sibling touch before a gated prod edit). (2) a source-scanning meta-test's first `strings.Index(src,"default:")` matched an earlier `default:` → false positive; fixed by anchoring the scan to the `func applyEdit(` body (scope a source-scanning meta-test to its TARGET function). Own close ran no consolidation.
- 2026-07-25 — Stack-agnostic planning & execution; version 1.8.0 (spec 0034): closes field finding #5 (planning/execution prose was Go-shaped; the shipped template copied Go idioms into every host repo). The EXECUTION substrate was already stack-aware (go/python/js/ts via AdapterForLanguage+cfg.TDD.<Lang>.Command; rust via the separate AdapterFor+cfg.TDD.Rust.Runner) — the gap was the authoring layer. New `DetectStack(root,cfg) Stack{Language,TestCommand,TestPatterns[],InlineTests}` (tools/internal/speccraft/detect.go) probes ONLY exact repo-root manifests; polyglot precedence is fixed data go > rust > python > ts > js (compiled-lang manifests rank above package.json; ts refines js by tsconfig.json). Two subcommands: `speccraft-state detect-stack` (versioned {"schema":1,…} JSON, exit 0 incl. unknown, non-zero only outside a repo) and `test-command` (effective command as DATA; non-zero when empty). A single-line `<!-- speccraft:test-command = "…" -->` conventions.md marker (per-line quoted-string regex, first-of-dupes wins, empty/malformed→detection fallback, verbatim never shell-eval) OVERRIDES detection and is the editable source of truth; `/speccraft:init` seeds it (commands/init.lib.sh::seed_conventions — preserve-existing/idempotent, unknown→TODO+empty marker). The four authoring docs rewritten to reference the project's command; two mechanical meta-guards (authoring_prose_test.go labeled-example-or-test-command rule; template_purity_test.go) promote the template-purity guardrail from advisory to executable. Version 1.7.1→1.8.0; go test + bats green. TWO environmental findings: (1) ONE pre-authorized override (T1) to bootstrap the brand-new DetectStack/Stack symbol — the AC13 new-symbol limitation 0031 predicted; the JSON-envelope trick doesn't apply to a wholly-new type; the marker parser was put in package main to avoid a SECOND bootstrap. (2) The hook ran the STALE 1.1.0 cached guard (pre-0031 Write blind spot: reads new_string, Write sends content) → every RED was registered via Edit, not a fresh Write. Deviations: init seeding landed at top-level commands/init.{md,lib.sh} (not commands/spec/); seed_conventions takes <root> <template>; Go TestCommand surfaces the SUITE form go test ./.... Own close ran no consolidation.
