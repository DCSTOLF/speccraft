# History

Append-only. Newest first.
## 2026-09-25 — Build-repair mode: a broken build no longer forbids its own repair, and red candidates survive a no-new-test edit (spec 0048)

**Spec:** specs/0048-guard-wedge-red-candidate-clobber/ (status: closed; `related-specs: 0018, 0031, 0032, 0047`; no `domains:` key — routing seeded at consolidation). Two defects in the spec-0018 red-check path, both found by dogfooding, both making `speccraft-guard` block edits it had no business blocking, and both already papered over with avoidance advice in `.speccraft/conventions.md` — which is the tell that they were product defects and not user error. **Defect A:** an adapter result of `runner.OutcomeBuildFailed` returned an error, so the edit was blocked. The rule ("a build failure is not a valid RED") is right; its blast radius was not. When the break is in **production** code, every later production edit in that package hits `OutcomeBuildFailed` — *including the edit that repairs the break*. The tree is unbuildable and the guard forbids making it buildable; the only exit was `/speccraft:spec:override`, and spec 0047 stated a budget of 0 and spent **5** on exactly one such forward reference. **Defect B:** `captureRedCandidates` computed `added = postIDs − preIDs` against the file's current disk content and handed it to `SetRedCandidates`, which overwrites per file. So any edit to a test file adding no new `func Test…` — an import, an assertion, a comment — yielded `added = []`, replaced that file's standing candidates with the empty set, and re-blocked a production edit backed by a genuinely failing test. Both fail the same way: they present as "no failing test observed", i.e. as the author's error, while `state.json` plainly shows the candidate that was just erased. The shared root cause is that the guard reasoned about **one edit against current disk** when the invariant it enforces is about the **session**.
**Decision:** Fix both, and amend the guardrail out loud rather than widening it quietly. **A — bounded build-repair mode, Go only.** `OutcomeBuildFailed` now calls `buildRepairBranch`, which applies the *proposed* edit in memory via the existing `applyEdit` (so all four gated write tools are modelled, specs 0031/0032) and probes that content against a scratch `-overlay`. Four outcomes, decided in order: (1) **no prober for the language** → today's refusal, produced by the one `buildFailedBlockError` so AC7's "unchanged" is true by construction rather than by a second copy that can drift; (2) **the probe cannot complete** — overlay write, toolchain, or the 30s `SPECCRAFT_BUILD_PROBE_TIMEOUT` deadline — → **BLOCK**, naming both the original build error and the probe failure, because a probe that cannot answer must never be read as "clean"; (3) **overlay builds clean** → the edit REPAIRS the break: clear the attestation, allow silently, and the ordinary red-check re-arms on the next edit; (4) **overlay still broken** → allow, but **record first**, against a session-wide budget of `buildRepairMaxEdits` = 10, the 11th blocked naming `/speccraft:spec:override`. The cap is enforced **inside the atomic record operation** — `RecordBuildRepair` checks it under the same `mu.Lock()` that appends the entry and opens the attestation — because that is the only placement where "recorded before allowed" is race-free; checked outside, two guard invocations each see 9 entries and both append. The budget is derived **solely from `len(Session.BuildRepair)`**: the attestation deliberately carries no counter (one there could diverge from the log), and a clean probe clears the attestation but **never** the log, so break → spend → repair → break again cannot refund what was spent. Only `ResetSession` resets it. Relatedness is **not** inferred (a break in file A is routinely repaired in file B), so an unrelated edit **is** admitted while the tree is broken — AC6 pins exactly that — and the claim is deliberately narrow: at most 10 such edits can exist in a session, every one is recorded before it is allowed, and `speccraft-state build-repair-log` surfaces them at close, informationally, always exit 0 even on a read error (a report that could fail the close would make honest multi-step repair look like failure, and the rational response would be to avoid repair mode — putting the wedge back). Because this is a **second** bypass class alongside `/speccraft:spec:override`, `.speccraft/guardrails.md` says so in the same pass, and the number in that sentence is pinned to the compiled constant **bidirectionally** by a test anchored on the carve-out *sentence* rather than on a bare numeral — changing the constant fails, and deleting the bound from the prose fails too. `conventions.md` was amended together with it: its "do not fix this by relaxing the rule" note was 0048's own deferred follow-up, and a stale prohibition sitting next to a sanctioned exception is worse than either alone. **B — a first-touch-only baseline.** `CaptureRedCandidates(root, file, preIDs, postIDs)` sets `red_baseline[file]` from the pre-edit ids **iff absent** and recomputes `red_candidates[file] = postIDs − baseline`, both under one lock and one save. First-touch is `_, ok :=`, never a length check — a brand-new test file whose baseline is legitimately empty has still been touched, and re-deriving it next edit reintroduces the bug. A no-new-test edit now recomputes the same set; a genuine deletion still shrinks it. The capture error is now **returned**, which makes a failed capture **block the test-file edit** — the one case where a test-file edit is refused, and deliberate: the guard runs in `PreToolUse`, so refusal leaves disk byte-unchanged and the natural retry re-captures from identical content. Allow-and-mark-poisoned cannot recover, because once the edit lands disk holds the new test and any later capture baselines it away; that was round 3's convergent finding. One canonical `NormalizeStateKey` (abs → `EvalSymlinks` of the deepest existing ancestor → rejoin the non-existent tail → `Clean`) keys candidates, baselines and log paths, applied on **both** sides of the sibling lookup.
**The probe is ONE command, and finding that out corrected the plan's own correction.** Plan correction C1, recorded before any implementation, caught that the spec's `go build -overlay ./...` is insufficient: `go build` does not compile `_test.go` files, while the adapter that produced `OutcomeBuildFailed` runs `go test` — so a broken **sibling test file** yields a clean overlay, AC1 fires, and every later Go production edit is admitted silently and unlogged for as long as that file stays broken. That is the unbounded allowance rounds 1–3 of review were spent closing, reintroduced through a mechanism detail rather than through policy, and none of the 18 ACs caught it. C1 therefore specified **two** commands. Implementing it surfaced the converse: `go build -o <dir> ./...` **fails with "no main packages to build" on a library-only module**, so keeping it would have reported every edit to such a module as still-broken and pushed it into repair mode — a false positive on the most ordinary case in this very repo. `go test -overlay … -run '^$' -count=1 ./...` **alone** compiles non-test *and* test files (verified directly: including for packages with no test files at all), runs nothing, and links into a temp `GOCACHE` rather than the tree — so it needs no `-o` and cannot drop a binary into the author's repo the way a bare `go build` can. One command is strictly safer *and* strictly more thorough than the two it replaces; C1's intent is fully preserved and the command it added is the one that survived. `GOFLAGS=-mod=readonly` guarantees the probe cannot rewrite `go.mod`/`go.sum`; nothing under the repository root is created, written or removed, asserted by a recursive pre/post snapshot across all three outcomes. There is deliberately **no build-tag split** — `-overlay` is portable everywhere Go runs, and spec 0047's process-group hang is solved with `exec.CommandContext` + `cmd.WaitDelay` instead of unix-only `Setpgid`, evidenced by `GOOS=windows` and `GOOS=darwin go build ./...` both green.
**Divergence (honest).** (1) **Override budget: stated 1, actual 3 — and none where the plan expected.** T2, the from-scratch exported bootstrap the single override was budgeted *for*, landed at **zero**: T1's RED was authored as **literal-JSON round-trips** through `LoadState`/`SaveState`, so the package kept compiling and the failure was a genuine runtime one — the spec-0031 envelope-boundary technique applied to struct *fields*, which drops an internal bootstrap from one override to none. All three were spent on T9 and T14, and **two of the three were this spec's own defects biting their own fix.** Defect B reset the test file's candidates after an edit that added no new `func Test…` and the guard refused the repair (**fourth** occurrence in the session; the first three were routed around by adding genuinely useful tests, and a fourth invented test would have been padding). Then `siblingRedCheck` turned out to index `red_candidates` in **two** places — once gathering the just-added set, once deciding which ids to RUN — and only the first was normalized, so the fix cost a second EDIT and `ConsumeOverride` is per-edit: spec 0049's lesson, applied one task late. Finally T14 wired the branch to call `buildRepairBranch` before writing it, breaking the build so the guard forbade the edit that completes its own repair — **defect A, first-hand, while fixing defect A** — against a plan whose T13 exists solely to enforce stubs-before-callers. Defect B fired four times and defect A once during their own repair, which is the strongest evidence available that both were worth fixing. (2) **`ToolInput` is carried on `deps`**, not threaded through `siblingRedCheck`'s signature as plan §Step 14 specified: that signature has **six** call sites, changing it is only safe as one atomic edit, and any intermediate state leaves the package uncompilable — at which point the red-check returns `OutcomeBuildFailed` and the guard forbids the rest of its own change. The cost is mixing request data into a dependency bag. (3) **The routing source-scan is split per package.** The plan had one test in `internal/speccraft` reaching into `cmd/speccraft-guard` by file path; beyond brittleness, **the red-check is package-scoped**, so a structural RED in one package cannot authorise an edit in another — discovered when the guard refused T11 with a perfectly good failing test sitting in the wrong package. (4) **T16 needed no code change** and is recorded as a PIN set rather than a fabricated RED→GREEN. (5) **Two predicates could not fail.** T25's asserted the absence of `must not be relaxed`, a phrase that never appeared in `conventions.md`; T15's record-failure injection made `.speccraft` a **regular file**, which makes the prologue unable to resolve an active spec and therefore **allow** the edit — passing for entirely the wrong reason. It now makes `state.json.tmp` a directory, breaking only the save, and the internal-package half reuses spec 0035's existing `atomicRename` seam instead of adding a second one, which is why `saveStateLocked` now routes its rename through it. (6) **The spec text still describes the two-command probe**; closed-spec immutability applies, so it is recorded in `changelog.md` rather than edited. (7) `started_at_sha` was set here, so this close's diff is accurate and self-scoping — spec 0049's was empty and its file list had to be reconstructed by hand.
**Review.** Three rounds, cross-model, all six outputs distinct md5s — **no false quorum.** codex `changes-requested` ×3; claude-p `changes-requested` → `approve` → `approve`; recorded as `approve-with-comments` because codex never withdrew. Round 1's headline, reached independently by both: the draft's "overlay still broken → allow" branch was an **unbounded bypass**, and the draft's defence — that an unobservable signal is not really a bypass — did not survive contact: unobservable-therefore-allow-everything is not a narrower rule, it is no rule. That produced the bounded, self-closing, logged mode that shipped, plus the three probe parameters AC1/AC2/AC3/AC5 had no oracle without, and the close-time consumer without which the spec would have shipped state nothing read. Round 2's sharpest catch was that the bound was **per-episode, not per-session** (break → 10 → repair → break → 10 more is unbounded in aggregate), plus two honesty corrections: the claim that feature work cannot pass through repair mode was false, and the fingerprint constrained nothing. Round 3 converged on a real logic error in the poison-recovery text — a next-touch retry cannot restore the candidate, because that touch baselines already-poisoned disk content — which is why capture failure now blocks. claude-p judged the carve-out acceptable **as policy** and named what would change its mind: evidence the 10-edit cap is hit routinely, a Go pre-edit gate at comparable effort, or evidence the close-time report goes unread. codex's alternative — routing still-broken allowances through `/speccraft:spec:override` — does not reduce the number of bypasses, it relabels them, and spec 0047's 0→5 overshoot is evidence the override budget is exactly what the wedge blows through.
**No release.** Ships unbumped at 1.16.0; a later release spec carries 0047, 0048 and 0049 together. `go test ./... -count=1` (8/8) + `go vet` green, **333 bats** green (with the freshly built `./bin` prefixed to PATH per spec 0049; `tasks-verify.bats` run separately, since it invokes the verifier executing the predicate), drift clean, `GOOS=windows`/`darwin go build ./...` green. Out of scope and still open: probers for Python/JS/TS — each needs its own overlay equivalent, and until then those languages keep the unmodified blocking behaviour, so **the wedge is still live for them**; a Go pre-edit gate mirroring Rust's `RunPreEditGate`; gating anything on the build-repair log; and retiring the two workaround conventions these fixes supersede.

## 2026-09-25 — Command-lib portability: three silent GNU-only no-ops fixed, the status enum becomes artifact-kind-scoped, and the bats suite runs on macOS (spec 0049)

**Spec:** specs/0049-command-lib-portability/ (status: closed; `related-specs: 0022, 0025, 0029, 0030, 0036`; `domains: cross-env-portability`). A field report against installed plugin 1.6.1: `/speccraft:arch:decide` reported success, left the design at `status: draft`, and dropped a stray byte-identical `design.md-E` sibling. One line was responsible — a `sed -i -E "0,/^status:/…"` in `commands/arch/decide.lib.sh` with two independent GNU-isms: BSD sed reads `-i`'s next argument as the backup suffix (hence the `-E` sibling) and does not support the `0,/re/` address form. Nothing was substituted, and because that `sed` was the function's LAST command its exit status of 0 became the function's. **The silent-success half is the real defect** — a loud failure would have been a nuisance; this one advanced the workflow on a lie. The sweep it prompted found the same failure class twice more: `commands/pm/prioritize.lib.sh:25` was byte-identical in the substantive part (so `/speccraft:pm:prioritize` no-opped identically), and `commands/spec/consolidate.lib.sh:409` reversed history entries with `tac`, absent on macOS — a missing command inside a process substitution yields empty input, the `while` body never runs, and `set -euo pipefail` never fires because a process substitution's exit status is never checked, so `consolidate_backfill_order` silently discarded its entire history-chronological half and spec 0025's AC11 ordering contract was void on macOS with no error. macOS is a primary Claude Code platform, so all three are first-class correctness bugs.
**Decision:** Do NOT portability-patch the `sed`. A hand-rolled frontmatter write is exactly what spec 0036's guardrail forbids, so a portable `awk`-to-temp rewrite would merely re-earn the violation under a widened guard. Both helpers keep their `draft`-only source gate and **delegate the write to `speccraft-state set-status --kind design|brief`**, inheriting the byte-safe `setFrontmatterField`, closed-artifact immutability, and — the point — a real non-zero exit. That forced the enum: spec 0036's flat `validStatuses` is the SPEC state machine and contains neither `decided` nor `prioritized`, so it becomes **`kindStatuses`** keyed on a new string-backed `ArtifactKind` (spec `draft/reviewed/planned/in-progress/blocked/closed`, design `draft/decided/closed`, brief `draft/prioritized/closed`). A flat map widened with the two new values was explicitly rejected — it would let a `spec.md` be set to `decided` and dissolve three distinct state machines into one. `SetStatus(path, kind, status)` replaces the two-arg form at all nine call sites with **no compatibility wrapper**, because a wrapper that defaults the kind IS the silent-default surface this spec removes; `ArtifactKind` has **no meaningful zero value** (the kinds are explicit non-empty constants, and the empty/unknown kind errors BEFORE status validation, so an uninitialised kind is a loud failure rather than a silent reinterpretation as `spec`). The one convenience default — no `--kind` ⇒ `KindSpec` — lives in the **flag-parsing layer only** (`tools/cmd/speccraft-state/set_status_cmd.go`, cmd package, riding the compile-stable `run()` seam), visible and one hop from the usage string, never inside the library. `--kind` validates only the status enum, never that the path really is an artifact of that kind: the caller knows the kind, and path-sniffing would be a second, weaker authority over artifact identity (pinned by a test that `--kind design <brief.md> decided` succeeds, so the choice is recorded as deliberate). Each helper resolves the binary `$SPECCRAFT_STATE_BIN` → `PATH` → plugin-local `bin/`, and the Linux `hooks` job now prepends the built `bin/` to `$GITHUB_PATH` because PATH wins over the plugin-local copy.
**Both guard gaps closed, and both had the same shape.** `.speccraft/conventions.md` already said command libs never hand-roll a `sed -i` frontmatter edit — but `tests/hooks/frontmatter-writer-guard.bats` scoped its path-shape filter to `spec\.md|SPEC_MD|spec_md`, and these two libs target a `design.md`/`brief.md` through `"$file"`. Spec 0022's PM/Architect artifacts were **never brought under the spec-0036 guardrail**: the rule was written for spec.md and the second and third artifact kinds arrived outside it. **A guard scoped to one artifact kind is a guard that will miss the next two.** The filter now covers all three literal shapes plus the non-literal holders, with the escaped line carried VERBATIM as a FORBIDDEN fixture so the guard is pinned against the bug that actually shipped rather than a reconstruction of it. **The scan root stays `commands/` and must never widen to `specs/` — that is load-bearing, not an oversight:** consolidation moves this very spec to `specs/.archive/0049-…/spec.md` carrying the forbidden line quoted in its Why section, so a `specs/**` scan root would flag the project's own historical evidence and wedge the guard on its own archive. Stated in the guard's header AND asserted by a test (with bracket-escaped patterns, `command[s]`/`spec[s]`, or the test matches its own source line and the assertion goes vacuous). The second gap was CI: `.github/workflows/ci.yml` already had a `macos-14` job, but it ran `go test ./...` only — the bats suite was Linux-only. `tests/hooks/arch-decide.bats` **already** asserted `arch_set_status: draft -> decided`, the exact transition that silently failed. **No new test was needed to catch this bug — only a runner.** A new `hooks-macos` job runs the FULL suite on `macos-14` through Homebrew Bash 5 (macOS ships 3.2), and `scripts/assert-no-gnu-userland.sh` runs as a workflow step BEFORE bats: path-shape rejection of `*/gnubin/*` and `*/coreutils/libexec/*` plus a behavioural `--version` probe, because a job greened by `brew install coreutils` would have passed against all three defects here. It is deliberately NOT a bats test — on Linux `tac` resolves legitimately, so a live assertion would invert and fail the Linux job; `tests/hooks/no-gnu-userland.bats` exercises the same logic against synthetic `$SPECCRAFT_PROBE_PATH` values. Alongside it, a new fixture-first `tests/hooks/portability-guard.bats` forbids eight GNU-only forms across the shipped surfaces (`hooks/ commands/ tools/ templates/ agents/ skills/`), prefix-safe (`sed -i` must not match `sed -i.bak` or `sed -i ''`), comment- and fence-blind, with **no escape hatch** — an annotation that silences a guard makes it evadable by annotation — and the cost (reword prose under a scanned surface, or move it to `.speccraft/`/`specs/`) accepted explicitly. The two mechanisms answer different questions: the guard asks "did we ship a GNU-ism to a user?", the runner asks "does our own suite run on BSD?" — which is why `tests/**` is outside the guard but its GNU-only EXECUTIONS were ported, not exempted.
**Review.** Three rounds, cross-model (codex + claude-p), converging: codex `changes-requested` → `changes-requested` → `approve`; claude-p `approve-with-comments` throughout. Six raw outputs, six distinct md5s — **no false quorum in any round**. Round 1's two verified-blocking findings were both ACs that no implementation could satisfy as written: AC2's read-only-file write-failure fixture (`setFrontmatterField` routes through `AtomicWriteFile`, and `rename(2)` succeeds into a writable parent regardless of the target's mode) → replaced by a deterministic seam per layer, a `PATH`-shimmed `speccraft-state` stub for the shell helper and spec 0035's `atomicRename` for Go, never permissions; and AC10's macOS suite, infeasible without a stated toolchain + PATH policy. One codex concern was checked and REJECTED with evidence (`history_parse_entries` emits one header line per entry, so line-reversal IS entry-reversal — no multiline byte contract at risk). **Round 1's synthesis deliberately refused the quorum count:** claude-p's `approve-with-comments` numerically satisfied `review_quorum = 1`, but marking a spec about eliminating silent-success-on-a-lie as `reviewed` while carrying two known-broken ACs would have been the same failure mode one level up the process. Round 2 was likewise not flipped to `reviewed` — that would have been self-certification on the orchestrator's own edits — so a round 3 over the five edits closed it, and claude-p's free-form "quorum is satisfied, flip the status" was correctly treated as an aux agent's opinion, not a determination. Eight implementation-level items were carried to `tasks.md` rather than re-edited into an approved spec; the sharpest (a no-GNU assertion that would invert on Linux) was a real trap avoided.
**Divergence (honest).** (1) **AC10's own claim was wrong, and the spec disproved itself.** It asserted "exactly one file, seven mutations"; the mechanised sweep that carried review item 8 required — precisely so the claim would be reproducible — showed the hand sweep behind it had searched `grep -P ` with a trailing space and missed `grep -qP`. True answer: **two files, nine sites** (seven `sed -i` in `spec-revise-preflight.bats`, two `grep -qP` in `init-workspace.bats`). Both ported, not excluded; AC10 amended in place; the claim is now asserted by `tests/hooks/tests-portability-sweep.bats` rather than stated in prose, so the next such error fails a test instead of shipping. (2) **Override budget: stated 3, actual 3, against a plan estimate of 1.** `ConsumeOverride` is per-EDIT, not per-task: the atomic enum migration touched two non-adjacent regions of `revision.go` (~110 lines apart — one Edit spanning both would have cost 2, not 1) plus the `main.go` call site. Budget exhausted exactly; recorded as an avoidable estimation error, not a surprise. (3) **Three inert assertions** — a bare `! grep -q` line (POSIX `set -e` ignores a `!`-negated failure, so the `@test` passed while the lib still held the forbidden token), a `;` mid-chain in a `done:` predicate, and a `|| true` that neutralised the single most important check in the spec. Found by making each negative assertion prove it bites; codified as a convention. (4) Spec 0048's `red_candidates` clobber fired live and blocked a GREEN edit; worked around per the documented recipe — a real, still-open defect in a `blocked` spec. (5) The optional `_arch_state_bin`/`_pm_state_bin` de-duplication was SKIPPED deliberately (spec 0022 T16 weighed the same trade for the same two files and kept them independently sourceable), and recorded as `[x]` not `[~]`: under `contract: done-means-v1` `parseTasksFile` accepts only `[ ]`/`[x]`/`[X]`, so a `[~]` line parses as prose and its `done:` line attaches to the previous task, making that task malformed and unbypassably exit 2.
**AC11 evidence.** `scripts/verify-linux-pins.sh` reverts each of the three production fixes in a detached worktree (never a temp clone, which can inherit an unclean index), fingerprints the caller's tree BEFORE any setup, restarts each mutation from the same clean baseline, traps cleanup on every exit path, and re-fingerprints at the end: `3/3 pins confirmed`, checkout byte-identical. No fix in this spec is pinned only by the macOS runner — so the documented fallback of gating `hooks-macos` to `push` on `main` cannot silently un-pin a real bug.
**No release.** Ships unbumped at 1.16.0 by explicit decision (Resolved decision 1): 0046 set the precedent of a dedicated release-only spec, and a later one will carry 0047, 0048 and 0049 together — so `feedback_bump_before_close` does not bind here, and the six version locations are asserted still-1.16.0 by a task predicate. `go test ./...` + `go vet` green, **330 bats** green, and `tasks-verify --run` exits 0 with all 22 `done:` predicates executing. Follow-ups: `commands/arch/close.md:36` and `commands/pm/close.md:24` still instruct a model to hand-edit `status:` → `closed` (the instruction-layer cousin of this bug; `--kind design|brief closed` now exists to fix it properly, but no AC covered it); a non-destructive `/speccraft:sync` pass that REPORTS stray `<file>-E` siblings with a recovery hint; and `stat -c` under `tests/e2e/` stays out of scope as the only remaining test-side exclusion.

## 2026-08-07 — Per-task done-means contract: tasks.md gets a decomposition grammar and close gets a mechanical completion gate (spec 0047)

**Spec:** specs/0047-done-means-contract/ (status: closed; `related-specs: 0035, 0036, 0045`; `domains: spec-lifecycle`). Field feedback from an agent that ran a full lifecycle under load: all four of its real misses were **a multi-part task ticked when its most visible part was finished** — one shipped a config knob declared but never read; two others were "implement the seam + write six live tests" ticked with the code written and zero tests. The cross-model review loop cannot catch this class (review finds *design* errors, not *"you marked it done and it wasn't"*), and nothing else looked either: `close.md` step 2 only asked whether every top-level box was `[x]` — precisely the assertion that was false. The deeper pattern, and the frame this spec argues from: speccraft's durable wins are all **mechanical** (the TDD PreToolUse gate, `set-status` refusing closed specs, the `state.json` write ban); its failures are all in the **instruction layer**, markdown a model is merely asked to follow.
**Decision:** Give `tasks.md` a **decomposition contract** and enforce the mechanically checkable part of it at close time. (1) **Grammar** — one level of `TN.<suffix>` sub-checkboxes (`[A-Za-z0-9]+`) plus a per-task `done:` line at exactly 2 spaces of indent, exactly one, non-empty. Recognition is **syntactic** and parent resolution comes **after**; that split is what keeps "sub-checkbox with an unresolvable parent" (exit 2) distinct from "an indented bullet that was never a sub-checkbox" (prose, ignored at any depth), which is how spec 0016's second-level `- [x] #1 …` bullets stay clean. Only **indentation** makes a sub-checkbox: a column-0 id may be dotted and multi-dot, because spec 0001 uses `T0.1` and `T0.5.1` there. (2) **Opt-in marker** `contract: done-means-v1`, versioned from day one so grammar evolution takes a new value rather than silently redefining an existing one; files without it (0001–0046 and the whole archive) are held only to the unambiguous checks. No date logic, no retrofit. (3) **Oracle** `speccraft-state tasks-verify <tasks.md> [--run]`, a case on the existing `run()` seam in the cmd package: exit `0` clean / `1` violations / `2` malformed, findings as escaped TSV `task-id`, `kind`, `detail`. **AC1 — a `[x]` parent with a `[ ]` sub-checkbox — fires on EVERY tasks.md, contract key or not**; that is the check that would have caught all three premature ticks. The `done:` requirement fires only under the contract, for **every** task regardless of tick state, so an omission surfaces at plan time rather than after completion. (4) **Predicates** — a `done:` value starting with `$` is executable under `--run`: `/bin/sh -c` by absolute path (POSIX, not bash), CWD = repo root via `FindRoot` (never the tasks.md dir, never the caller's), env inherited, stdin `/dev/null`, stdout+stderr combined and capped at 4 KiB with `…[truncated]`, timeout `SPECCRAFT_TASKS_VERIFY_TIMEOUT` (Go duration, default 30s; unset/empty/invalid/zero/negative → default) mirroring the spec-0045 `parseLockTimeout` precedent, and the timeout kills the **whole process group** — killing only the `sh` leader leaves a forked grandchild holding the captured pipe and hangs the verifier on exactly the runaway predicate the timeout exists to bound. Unticked tasks are never executed. A bare `done: $` with no command is malformed, not prose. (5) **Gate** — `close.md` step 2 runs `tasks-verify --run` in place of the eyeball check, before the diff and before `memory-keeper`, so a doomed close does no downstream work; bypass requires the literal `SKIP-TASKS-VERIFY` (a blanket "approve all" does **not** qualify — that is the mode the original failure shipped in), exit 2 is never bypassable, and a bypass records the skipped findings in `changelog.md`. (6) **Producers** — `tdd-planner` emits the contract by default, including the knob-must-prove-its-**reader** predicate shape (`$ grep -q KNOB <definer> && grep -q KNOB <reader>`), and `spec-format` documents the grammar plus the previously-undocumented `domains:` key.
**Design note.** An earlier draft added a prose instruction to `memory-keeper` ("for each new identifier in the diff, name its reader"). Both reviewers independently flagged that this re-introduces the exact instruction-layer mechanism §Why indicts — the spec would have argued against prose-you-hope-the-model-follows and then added some. **Removed.** The unwired-knob class is covered mechanically by the two-site predicate above; a mechanical definition-without-reader check is deferred to its own spec.
**Divergence (honest).** **Override budget: stated 0, actual 5.** AC11's design constraint held — `tools/internal/speccraft/` is byte-unchanged and the subcommand rides `run()` — but the budget prediction did not: a forward reference to `runPredicates` broke the build, and the guard's red-check then refused every Edit and Write *including the repair*, costing 5 overrides to unwedge. Root cause was self-inflicted; the absent "this edit repairs the build" affordance is a real guard defect and is filed. Also: **T4/T6/T8 are pins, not REDs** (that unwedging forced the `done:` parser and the whole predicate runner to land ahead of their tests), and the tasks are retitled so the archived `tasks.md` does not misrepresent them; the genuine REDs were the structural layer, the two column-0-dotted-id tests, and the five close-pass tests. And **§Grammar's first column-0 rule was wrong** — it assumed a dotted id implied a sub-checkbox, and the archive sweep failed on spec 0001 (1 of 44 files) until only indentation was made load-bearing.
**Review.** Two rounds, cross-model (codex + claude-p). Round 1 produced six convergent findings — AC9's gate-ordering self-contradiction, a wholly unspecified predicate runtime (both citing spec 0045 as the precedent to mirror), the memory-keeper prose backstop contradicting the spec's own thesis, the unnamed `run()`-seam override budget, the one-frontmatter-parser routing, and an underspecified grammar — plus codex's strongest single finding: `close.md`'s blanket-approval path would have let the new gate be bypassed silently *in exactly the mode the field agent ran in*. Round 2 caught a real §Grammar-vs-AC8 contradiction (resolved by the recognition/resolution split), empty and duplicate `done:` lines satisfying AC2 while articulating nothing, the leader-only kill, and unbounded `detail`; an empirical audit of all 43 existing files then showed that **both** reviewers' proposed alpha-only suffix rule would have broken on the real `T1.10`. Stopped at 2 rounds by explicit decision — the residue was pin-a-constant refinement, not design.
**Process defect found by this review (not a spec finding).** Round 1's dispatch returned **byte-identical** output from both reviewers: both `aux-delegator` invocations wrote the same unqualified temp path (`/tmp/0047-review-output.txt`), so the second reported the first's bytes as its own verdict — a silently manufactured **false quorum**. Caught by md5-comparing the two bodies; claude-p was re-run with an isolated path, and the recorded verdicts are from that corrected run. Parallel aux review has no per-agent temp-file namespacing. Cross-model review is the plugin's headline feature and a false quorum is its worst possible failure mode — **filed as the highest-priority follow-up**.
**Close-time verification earned its keep.** `memory-keeper`'s adversarial summary-vs-diff pass found six real defects, all fixed before closing rather than shipped: AC9/AC10 had **no durable regression coverage** (the only check was a `grep` predicate that runs once at this close and never again) → `tests/hooks/close-gate.bats`; a bare `done: $` was silently reclassified as prose, a ticked task that looked executable and was never run — **this spec's own failure mode inside its own tool** → now exit 2; `GOOS=windows go build ./...` was broken because the `//go:build unix` split shipped only one half despite §Risk promising to mirror the `ledger_flock_*` precedent "exactly" → counterpart shipped and pinned; AC7's escaping was unpinned because no finding `detail` carries the task description → re-driven through predicate output; `--help` never reached the boundary text and an extra positional arg was silently ignored → both fixed behind one shared usage function; and `spec-format`'s own worked example violated AC1 three lines above the text explaining that violation.
**No release.** Ships unbumped at 1.16.0 in the 0037–0040 style (bundle into a later release spec). `go test ./...` + `go vet` green, **294 bats** green, drift clean, `GOOS=windows go build` green, and `tasks-verify --run` exits 0 against this spec's own `tasks.md` — the gate passes on itself.

## 2026-08-04 — Release 1.16.0: ledger write-locking closes the concurrent-writer race (spec 0045)

**Spec:** specs/0045-ledger/ (status: closed; `related-specs: 0043, 0044`). Specs 0043 (workspace ledger/membership reconciliation) and 0044 (design consolidation + ledger-row archival) both shipped under an explicit **single-writer assumption** and *bounded-but-deferred* the residual concurrent-writer race: every ledger writer (`ledger-set`, `ledger-archive`, the conductor) does read → modify → atomic-rename with no mutual exclusion, so two writers reading the same bytes could each rename and the last silently clobbers the first's change. `ledger-archive --expect` closed the caller→op handoff but NOT the op's own read→rename window — it is not a filesystem CAS. As the conductor and `/speccraft:sync` run more autonomously, concurrent ledger writes become plausible, and a lost update to the ledger is a silent correctness failure. 0045 is the load-bearing concurrency fix only; two adjacent follow-ups (`/speccraft:sync --recursive` and `ledger.archive.md` compaction) were **split into their own specs**.
**Decision:** Serialize **every** ledger writer's *entire* transaction behind a host-level exclusive advisory `syscall.Flock(LOCK_EX)` on a sidecar `<workspace-root>/.speccraft/ledger.lock` (path via `FindWorkspaceRoot` so the lock always lands at the workspace root, never a member repo), and move each writer's **authoritative read inside the lock** so read → in-memory transform → atomic rename is one indivisible critical section. `ledger-archive`'s two-file transaction is held under the **same** lock end to end (both-present crash recovery → archive append+rename → live-ledger remove+rename → every error path). The `--expect` CAS is kept as a **semantic** check on the existing `reconcile | sha256sum` fingerprint but now re-derived from the ledger bytes read *under the lock* and compared before any write — an **in-process** computation (never a shell-out from inside the critical section) equal to what the pipeline would yield; no whole-ledger byte-identity claim is made (a reconcile projection cannot establish that). `ledger-set` intentionally takes no `--expect`: single-field write, serialization suffices, last-writer-wins on the same field is deliberate. New portable core `tools/cmd/speccraft-state/ledger_lock.go` (`withLedgerLock`, `parseLockTimeout`, the `ledgerLockHold` test seam) + a build-tag split (`ledger_flock_unix.go` for shipped linux+macOS, `ledger_flock_other.go` stub that fails loud on any non-unix build). Bound mechanism is a **`LOCK_NB` poll loop** (10ms interval), NOT a cancellation goroutine — chosen consciously because the poll loop cannot leak a late acquisition after `ledger busy` was already returned (codex S1). Timeout via `SPECCRAFT_LEDGER_LOCK_TIMEOUT` (Go duration; default `10s`; unset/empty/invalid/zero/negative → 10s fallback, codex S2); on contention beyond the deadline the writer exits non-zero and stderr **contains** `ledger busy` (substring, never exact-equality — claude-p S1). Crash-safe by construction: advisory, kernel-released on exit, so on-success/on-error/on-crash are the same exit clause and no stale lock ever wedges the next writer. Lock file 0644 under an ensured `.speccraft/`, never parsed as ledger content, git-ignored the same way `state.json` is. **Override budget 0** — every RED is a runtime failure of an already-compiling command; the sole new production symbol tests reference (`ledgerLockHold`) rides Step 2's GREEN as a passenger and is only referenced by Step 4+ tests. Happy-path byte-identity of `ledger.md` / `ledger.archive.md` is preserved (the only new on-disk artifact is the ignored `ledger.lock` sidecar).
**Review.** Three-round cross-model quorum (codex + claude-p, converging): round 1 (codex `changes-requested`, claude-p `approve-with-comments`) drove the load-bearing spec revisions — the `--expect` byte-CAS overclaim → semantic CAS re-verified under the lock (both reviewers); the archive-transaction lock scope extended to span the two-file sequence end to end (codex); the timeout interface pinned (both); real synchronization seam over wall-clock timing pinned in AC2 (both); portability scoped to POSIX / linux+macOS with Windows + network-fs explicitly OOS; lock-file lifecycle pinned (mode 0644, ensured `.speccraft/`, never parsed, gitignored, descriptor closed); workspace-root resolver named (`FindWorkspaceRoot`); the `ledger-set` vs `ledger-archive` asymmetry stated deliberately. Rounds 2–3 folded the in-process-fingerprint clarification (claude-p, load-bearing — the `reconcile | sha256sum` shell-pipeline wording in AC5 conflicted with AC1's no-shell-out-in-the-critical-section rule; resolved by stating the check is an in-process computation equal to what the pipeline would yield), the distinct-target scoping of AC2's "both land" (codex), the timeout-fallback matrix (codex S2), and the adversarial-symlink threat-model exclusion (codex). Zero guardrail/convention violations across all rounds. Final: codex approve-with-comments, claude-p approve.
**Release.** Minor bump 1.15.0 → 1.16.0 via the renamed-version-test technique across the six single-source locations (3 Go `const version` → `…Const1160`/`…Is1160`, stale-guard now rejects 1.15.0, NO override; `manifest_version_test.go` → `…VersionIs1160`; plugin.json + marketplace.json). `go test ./...` + `go vet` green; 282 bats green; drift clean; `-race -count=5` clean; all workspace e2e pass; binaries rebuilt report 1.16.0. Push + tag `v1.16.0` triggers the auto-tag → release.yml → verify-release pipeline (spec 0021). Follow-ups: `/speccraft:sync --recursive` and `ledger.archive.md` compaction are split into their own specs; cross-host / network-filesystem locking and Windows support are out of scope.

## 2026-08-03 — Release 1.15.0: workspace sync — design-level consolidation & ledger-row archival (spec 0044)

**Spec:** specs/0044-workspace-sync-design-consolidation/ (status: closed; `informed-by: design/0001`). Completes the workspace-sync arc (A+B in 0043, **C** here). 0043 reconciled the ledger but it only ever *grows* — every design the conductor drives leaves a permanent per-member section in `.speccraft/ledger.md` long after it is fully `done`.
**Decision:** Add a confirm-gated **design-consolidation pass (`W4`)** to `/speccraft:sync`'s workspace branch: for every design `reconcile` reports `done`, fold it into a durable colocated `design/<id>-<slug>/outcome.md` rollup and **archive its ledger rows** out of the live ledger into a sibling `.speccraft/ledger.archive.md` (same grammar, `ParseLedger`-valid) — bounding the live ledger as designs accumulate (workspace analog of spec consolidation). Three flagged OQs resolved with the user (colocated outcome.md via a `sync_resolve_design_dir` glob resolver, sibling archive file, `W4`-in-sync not a separate command). New sanctioned Go op **`speccraft-state ledger-archive <design> [--expect <fp>]`** — the single-read atomic authority: four-case contract on a **text-level presence check** (so `Reconcile`'s vacuous-done-for-absent never leaks — present+done→archive, present+not-done→refuse+name members, absent-live+in-archive→idempotent no-op, absent-both→`unknown design`), a byte-level **section splice** (untouched designs byte-identical, not a re-serialize), transactional **append-archive-first/remove-live-second** with both-present crash recovery, and an **`--expect` compare-and-set** closing the caller→op handoff. Lives in the cmd package (rides the `run()` seam) → **override budget 0**; `reconcileCmd` refactored onto a shared `reconcileOutput` so the fingerprint = `reconcile | sha256sum`. Five pure `sync.lib.sh` helpers (`sync_resolve_design_dir`/`sync_design_fingerprint`/`sync_design_rollup_body`/`sync_done_live_designs`/`sync_consolidate_design`) + the `W4` runbook. **Crash-safety** via a fingerprinted `consolidated:` marker: `outcome.md` written before archival; recorded iff its marker fingerprint equals the current per-design fingerprint (missing/stale → atomic rewrite), so the durable record never describes an older snapshot than the archived rows.
**Review.** Four-round cross-model (codex + claude-p, converging): codex drove the vacuous-done→four-case contract, the non-transactional move→append-first/remove-second state machine, the byte-identity overclaim→section splice, cross-run skew→fingerprinted marker, and the caller→op TOCTOU→`--expect` atomic CAS. The residual concurrent-writer race (op read→rename is not a filesystem CAS) is **bounded + deferred** — the identical single-writer limitation every ledger writer has, and 0043's ratified scope; a workspace-wide lock / `--expect-bytes` CAS across all writers is the follow-up. claude-p approve-with-comments.
**Release.** Minor bump 1.14.0 → 1.15.0 via the renamed-version-test technique (six single-source locations, NO override); `go test ./...`+`go vet` green, 282 bats green, drift clean, all four workspace e2e pass (new `workspace_consolidate_cycle.sh`), binaries report 1.15.0. Push + tag `v1.15.0` triggers the release pipeline (spec 0021).

## 2026-07-31 — Release 1.14.0: workspace sync — ledger + membership drift reconciliation (spec 0043)

**Spec:** specs/0043-workspace-sync/ (status: closed; `informed-by: design/0001-architect-lifecycle-orchestration`). `/speccraft:init --workspace` (0042) *creates* a workspace and `/speccraft:arch:orchestrate` (0039/0040) *drives* it, keeping the ledger current only while it runs. But a developer who closes a member spec by hand, runs `spec:implement` directly in a member, or adds/removes a child repo leaves the workspace's own memory (`.speccraft/ledger.md` + `workspace.yml`) silently wrong — there was no out-of-band "make belief match reality" pass, the workspace analog of `/speccraft:sync`.
**Decision:** Give `/speccraft:sync` a **kind-branch** (auto-detected via the 0042 `config-kind` oracle): `repo` runs the unchanged repo flow; `workspace` reconciles **ledger drift (class B)** + **membership drift (class A)**. The core insight — **ledger drift is out-of-band re-entry**: spec 0040's `orch_reentry` already answers "does the member's live status prove this phase completed?", so sync asks it for every member of every design and an `adopt` verdict means the ledger is behind reality. The MVP reuses the tested token machine (`orch_reentry`/`orch_status_token`/`orch_next_phase`) + `ws_detect_members` wholesale, adding one read-only Go oracle **`speccraft-state ledger-get [<design>]`** (raw stored pointer fields — `reconcile` only exposes *computed* status; rides the `run()` seam → **override budget 0**), a pure `commands/sync.lib.sh` (`sync_status_ahead`/`sync_stale_in_flight`/`sync_ledger_drift`/`sync_membership_audit`/`sync_apply_member_plan`), and a `commands/sync.md` branch with `<!-- speccraft:sync:repo|workspace -->` anchors. Contracts: tool-managed ledger fixes are auto-applied (via `ledger-set`) while curated `workspace.yml` + dangling spec refs are **advisory only**; live-run-**conservative** (clear an `in_flight` only when `orch_reentry` says `adopt`, never a not-yet-reflected marker); a conflict-safe per-member **plan** re-reads + byte-compares the row once before applying (row changed since detection → `conflict`, no write; residual TOCTOU window + true CAS deferred); structured 6-field findings parsed with `awk -F '\t'` (never `IFS=$'\t' read`, which drops the meaningful empty columns); parse-level ledger corruption fails `ledger-get` globally while a *parsed* bad-token row is a per-member `malformed-row` advisory (row isolated).
**Review.** Three-round cross-model (codex + claude-p, both converging on the top issues): codex opened `changes-requested` and drove real hardening — the detect→apply race → the AC10 per-member-plan conflict guard; findings-as-rendered-commands → a structured argv-safe record (apply passes field/value as argv, never `eval`s `<detail>`); the AC8+AC10 self-conflict → captured-snapshot batch apply with no precondition re-derivation; TSV empty-field parsing pinned to `awk -F '\t'`; parse-vs-semantic malformation split; bounded live-run claim. claude-p approved-with-comments throughout. Full trail in review.md (`reviewed_sha256` stamped).
**Release.** Minor bump 1.13.0 → 1.14.0 via the renamed-version-test technique across the six single-source locations (3 Go `const version` → `…Is1140`, stale-guard now rejects 1.13.0, NO override; manifest version test → `…VersionIs1140`; plugin.json + marketplace.json). `go test ./...` + `go vet` green; 270 bats green; drift clean; both hermetic workspace e2e (`workspace_init_cycle.sh` regression + new `workspace_sync_cycle.sh`) pass; binaries rebuilt report 1.14.0. Push + tag `v1.14.0` triggers the auto-tag → release.yml → verify-release pipeline (spec 0021). Follow-ups: class C (design-level consolidation/rollup) is the next spec; `--recursive` fan-out and a true `ledger-set --expect` CAS are deferred.

## 2026-07-29 — Release 1.13.0: init --workspace scaffolds a workspace root (spec 0042)

**Spec:** specs/0042-init-workspace/ (status: closed; `informed-by: design/0001-architect-lifecycle-orchestration`). Closes the last gap in the conductor arc: 0037–0041 taught the tooling to *read/drive* a workspace (`kind = "workspace"`, `workspace.yml`, `ledger.md`), but nothing *created* one — `/speccraft:init` only ever produced a `repo`-kind bootstrap.
**Decision:** Add a `--workspace` mode to `/speccraft:init` that WRITES the two artifacts the existing readers already parse — a `speccraft.toml` with `kind = "workspace"` and a canonical `workspace.yml` manifest — plus a workspace-flavored `.speccraft/` set. Readers (`config.go`/`workspace_topology.go`/`ledger.go`) are untouched. Deterministic mechanics are five pure `commands/init.lib.sh` helpers (`ws_arg_parse`, `ws_toml_body`, `ws_manifest_body`, `ws_detect_members`, `ws_write_root`) backed by 26 bats; the model-driven per-candidate approval prompt lives in `commands/init.md` and is fenced by a source-scan + the hermetic `tests/e2e/workspace_init_cycle.sh`. Two new `speccraft-state` oracle subcommands — `config-kind` (strict kind, gated on `.speccraft/` presence) and `find-workspace-root` — were required because `list-members` can't discriminate a workspace root from a repo root; both ride the `run()` seam so the whole spec shipped with **override budget 0**. Key contracts: lazy ledger (never created by init), manifest-before-toml write ordering (no orphan `kind = "workspace"`), curated `workspace.yml` preserved on `--force`, and an explicit `repo → workspace` migration refusal (out of scope).
**Review.** Two-round cross-model (codex + claude-p): round 1 `changes-requested` → 8 fixes (per-file `--force` matrix, AC3a/AC3b tier split, structural AC5 marker, pinned canonical `workspace.yml`, narrowed AC8, argument parsing, named helpers); round 2 confirmed all resolved and caught the AC9 two-file-atomicity overclaim (reworded to the honest one-directional invariant).
**Release.** Minor bump 1.12.0 → 1.13.0 via the renamed-version-test technique across the six single-source locations (3 Go `const version` → `…Const1130`/`…Is1130`, stale-guard now rejects 1.12.0, NO override; `manifest_version_test.go` → `…VersionIs1130`; plugin.json + marketplace.json). `go test ./...` + `go vet` green; 241 bats green; drift clean; binaries rebuilt report 1.13.0. Push + tag `v1.13.0` triggers the auto-tag → release.yml → verify-release.sh pipeline (spec 0021).

## 2026-07-29 — Release 1.12.0: bundle the design-0001 conductor arc (spec 0041)

**Spec:** specs/0041-release-1-12-0/ (status: closed). Minor version bump 1.11.0 → 1.12.0 packaging specs 0037–0040 (workspace topology, ledger+reconcile, the arch:orchestrate conductor, crash-safe re-entry), each of which closed without a bump by design. Six locations via the established single-source pattern: 3 Go `const version` (state/guard/drift) each bumped through the **renamed-version-test** TDD technique (`…Const1120`/`…Is1120`, stale-guard now rejects 1.11.0 — NO override), `manifest_version_test.go` → `…VersionIs1120`, and the two JSON manifests (plugin.json + marketplace.json). `go test ./...` + `go vet` green; binaries rebuilt report 1.12.0. Pushing + tagging `v1.12.0` triggers the auto-tag → release.yml → verify-release.sh publish pipeline (spec 0021) — not done here (nothing pushed).

## 2026-07-29 — Retroactively closed the genesis spec 0001 (speccraft v1)

**Spec:** specs/0001-speccraft-v1/ (status: in-progress → closed; status reconciliation, no code change). The project-genesis spec that bootstrapped the plugin (hooks, the three Go binaries, commands, subagents, `.speccraft/` memory, e2e harness) had sat at `in-progress` since 2026-05-09 with 58/69 tasks done — the 11 open ones all manual-verification / early-phase-e2e placeholders, long since superseded by the shipped `tests/e2e/run.sh` harness, the release/verify pipeline (spec 0021), and the mock-agent e2e. Closed to reflect reality (`active_spec` was already null; v1 shipped and was hardened by the 40 specs through 0040). Retrospective changelog at specs/0001-speccraft-v1/changelog.md.

## 2026-07-29 — Crash-safe conductor re-entry completes the design-0001 arc (spec 0040)

**Spec:** specs/0040-conductor-crash-safe-reentry/ (crash-safety follow-up to the 0039 MVP; status: closed). Additive shell + markdown (extends orchestrate.lib.sh/.md + the hermetic e2e). ZERO `/speccraft:spec:override`; full `tests/hooks/` (215) + `go test ./...` green.
**Decision:** Close the crash window design 0001 named — a phase whose delegated command SUCCEEDED but crashed before the ledger advanced — so re-entry never double-allocates a spec or re-closes a closed one. On resume, before re-dispatching an `in_flight` member, inspect the member's real `spec.md` status and **adopt** (jump the pointer to the artifact's token) or **reattempt**. Six new pure lib helpers: `orch_status_ordinal`/`orch_status_token`/`orch_phase_completion_status` (the status↔ordinal↔token maps), `orch_reentry` (adopt iff member-ordinal ≥ phase-completion-ordinal; `""`/`blocked` never adopt; adopt jumps to `orch_status_token(status)` so an out-of-band `review`+`closed` lands on `validated`, not a partial replay), `orch_find_member_spec` (crash-safe `new` adoption keyed on the **`informed-by: [design/<id>]`** frontmatter `spec:new` actually writes — NOT a `design:` field; ≥2 matches or a get-frontmatter read failure → error, never a false zero), and `orch_in_flight_phase` (bare 0039 token or first `phase=<p>`; malformed → error). Runbook gains create-if-absent seeding (never re-`spec ""` on an existing row — the restart-clobber fix), a `new`-first re-entry resolution, and structured `phase=review iteration=<n>`. Three hermetic e2e crash legs (each a fresh workspace) prove it DIRECTLY: no-re-close (validate-mock dispatch sentinel == 0), no-double-allocate (`new`-mock sentinel == 0 + dir-count + non-empty ledger spec), restart-safety.

**Divergence (honest).** Literal spec-id-BEFORE-dispatch is infeasible without changing `spec:new`'s allocation contract (member repos number independently); superseded by post-hoc `orch_find_member_spec` adoption + write-ref-asap. This closes the crash window; it does not deliver id-before-dispatch.

**Review.** Cross-model (codex changes-requested → resolved; claude-p approve-with-comments; both invoked cleanly after the agents.toml fix) caught the load-bearing `design:`→`informed-by` key error, the seeding-clobbers-ref-on-restart bug, and the missing `new`-dispatch sentinel; the self-check caught the adopt-by-one multi-phase bug (→ jump to artifact token) and the `in_flight=new` ref-drop (→ `new`-first precedence).

**Arc complete.** 0037 topology → 0038 ledger+reconcile → 0039 orchestrate command → 0040 crash-safe re-entry. The design-0001 conductor is buildable, resumable, failure-isolated, and crash-idempotent. Deferred: concurrent-conductor locking; a release bump can now bundle 0037–0040.

## 2026-07-29 — Architect conductor ships: /speccraft:arch:orchestrate drives the per-member lifecycle (spec 0039)

**Spec:** specs/0039-arch-orchestrate-conductor/ (Spec B orchestration surface of design 0001; status: closed). Shell + markdown (not Go): a pure `commands/arch/orchestrate.lib.sh` + a `commands/arch/orchestrate.md` runbook. 28 new bats tests (`tests/hooks/arch-orchestrate.bats`); full `tests/hooks/` (192) + `go test ./...` green. **ZERO `/speccraft:spec:override`** — shell/markdown are ungated by the TDD guard; bats supplied RED-before-GREEN.
**Decision:** Ship the conductor that *sequences* the existing `/speccraft:spec:*` commands across a `kind: workspace`'s member repos, cwd-scoped per member (never `--root`), resuming from the ledger pointer and rolling up via 0038's `reconcile`. Deterministic core = bats-tested lib helpers: `orch_next_phase`/`orch_completed_token` (the token machine `new→reviewed→planned→implemented→validated`, `revise` loops within `review` and never advances), `orch_should_pause` (checkpoint a: after `planned`, before `implement`; suppressed by `--straight-through`), `orch_review_verdict` (pass/revise/escalate — checkpoint b), `orch_parse_decomposition` (first-tab split, indented-`#` comments, safe member-path charset `^[A-Za-z0-9._/-]+$` rejecting a literal `'`), `orch_dispatch` (`(cd '<m>' && /speccraft:spec:<...>)`, six pinned commands incl. `validate→spec:close`, single-quoted, never `--root`), `orch_validate` (`sh -c` tests gate), and `orch_apply_result` (failure-isolated ledger transition via `ledger-set`). The runbook seeds member ROWS only (no double-`new`), resumes, dispatches, gates validate→`spec:close`, reconciles.

**Design fold + scope honesty.** `answer-questions` is folded into `spec:new` (so tokens are five, not six); `validate` culminates in `/speccraft:spec:close` (the step that writes the `closed` status 0038's reconcile keys `Done` on — the self-check's critical catch). Cross-model review reframed this from "final slice" to the orchestration **MVP**: it ships resume-at-pointer + `in_flight` visibility (cleared on EVERY completed attempt) + blocked-set/clear, and **explicitly defers full crash-window idempotency to a follow-up (0040)** — design 0001 itself lists crash-safety as mechanism spikes #1/#4, not a settled contract.

**Dogfood.** No guard gymnastics (not Go) — bats is the RED oracle; behavioral AC6 tests build `./bin/speccraft-state` and drive a real `kind=workspace` ledger. The zsh/bats-reserved `status` name is never assigned in the lib. Two aux-config nits logged (agents.toml: codex `--sandbox workspace-write` one-token; claude-p argv payload overflow → use stdin).

**Deferred.** Full crash-safety → spec 0040; real-subagent e2e (mock-agent harness) + spike #1 runtime FindRoot → follow-up e2e; consolidation → `/speccraft:sync`; no version bump (a release step can now bundle 0037–0039, the whole design-0001 arc).

## 2026-07-28 — Conductor primitives ship: ledger.md read/write + reconcile rollup (spec 0038)

**Spec:** specs/0038-conductor-ledger-reconcile/ (Spec B slice 1 of design 0001; status: closed). Test-first — 22 new test cases, full `go test ./...` green, `go vet` clean, ONE `/speccraft:spec:override`.
**Decision:** Ship the Go/testable core the conductor (Spec 0039, `/speccraft:arch:orchestrate`) will stand on, before any orchestration — mirroring how 0037 shipped the topology primitives. New `tools/internal/speccraft/ledger.go`: `ParseLedger`/`SetLedgerField` over a constrained `## design`/`### member` block grammar (first-`:` split preserving interior `: `, BOM/CRLF tolerance, `# Ledger` preamble, first-wins, 11 `ledger.md:`-prefixed parse-error classes); a **canonical writer** on `AtomicWriteFile` with an injectable `ledgerNow` clock seam (same-value set = byte-identical no-op; `updated` is conductor-managed, never a settable field); and a **pure `Reconcile`** taking an INJECTED status resolver so it keys on the member's `spec.md` frontmatter (proven by a disagreement test AND a source-scan that the impl never references `last_completed_phase`) — classifying Blocked→Closed→InProgress with `blocked`-overlay precedence, `Done` iff all Closed. Two `run()`-seam subcommands (`ledger-set`, `reconcile`) at zero override; the ledger is a history.md-class memory file, NEVER `state.json` (behavioral + source-scan guards).

**Override budget.** ONE (T1 bootstrap of the exported inventory), exactly as planned. The load-bearing move: a shared `resolveSpecStatus` (dual live/`.archive`, tri-state outcome) was extracted from 0037's `get-status` and `get-status` refactored onto it at ZERO override — the pure refactor rode the standing failing `reconcile`/`ledger-set` cmd REDs (author cmd REDs first, land the refactor while they stand). 0037's get-status regression stayed green.

**Dogfood friction (anticipated by the plan).** (1) A second Write of a test file with unchanged test names disarmed its red-candidates (`SetRedCandidates` replaces per file) → re-armed via an Edit adding a fresh RED. (2) A literal BOM in Go source (from a `﻿` fixture) was fixed mechanically via Bash `perl` — the sanctioned path for a build-break the guard misreads. (3) The AC3 source-scan first caught `state.json` in a ledger.go comment; reworded.

**Deferred.** Consolidation → `/speccraft:sync`; no version bump (Spec 0039 — the orchestrate command — completes Spec B and carries the release). Aux-config nit logged in the spec's review.md: `.speccraft/agents.toml` codex `cmd` should split `--sandbox workspace-write` into two tokens.

## Compacted (older than the active window)

### Architect conductor foundations (design 0001)
Specs: design/0001, 0037
Archive: .speccraft/history-archive/history.md
Design decided (2026-07-28) to graduate the architect from an advisory design-writer into a conductor that sequences the existing per-spec lifecycle (`new → answer-questions → (review → revise)* → plan → implement → validate`) across one or many member repos without bypassing the per-spec commands or the TDD guard. Chosen by composition over new primitives and advisory over blocking to preserve member autonomy and keep single-repo behavior untouched. `.speccraft/` gains `kind: repo | workspace` (absent ⇒ `repo`, full backward compat); a `kind: workspace` root holds `workspace.yml` + a ledger but no specs of its own; membership is manifest-authoritative (unlisted repos ignored, missing paths become a `blocked` overlay). New `FindWorkspaceRoot` sits as a peer of `FindRoot` layered ABOVE it, consumed only by `arch:*`/the conductor, so hooks/`speccraft-guard` never touch the Edit/Write hot path. Spec 0037 shipped the topology primitives — the `kind` field via `parseSpeccraftTOML` with a strict/coerce split (`ReadConfigStrict` errors on unknown values so a bad value can never silently promote a repo to a workspace), `ParseWorkspaceMembers` (hand-rolled YAML subset, presence-preserving), the `get-status`/`get-frontmatter`/`list-members` readers on the `run()` seam, and a source-scan guard keeping `FindWorkspaceRoot` off the hot path — the design's Spec A, deliberately released before the conductor could exist.

### Conductor delivery arc: ledger, orchestrate, crash-safe re-entry, workspace init
Specs: 0038, 0039, 0040, 0041, 0042
Archive: .speccraft/history-archive/history.md
Design 0001 delivered as a four-spec arc plus a workspace bootstrap. 0038 shipped the Go/testable ledger core: `ParseLedger`/`SetLedgerField` over a constrained `## design`/`### member` grammar, an `AtomicWriteFile`-backed canonical writer with an injectable `ledgerNow` clock seam, and a pure `Reconcile` taking an INJECTED status resolver so it keys on the member's `spec.md` frontmatter (proven by a disagreement test) — with the load-bearing boundary decision that `ledger.md` is a history.md-class memory file DELIBERATELY OUTSIDE the `state.json` single-writer rule (behavioral + source-scan guards). 0039 shipped the `/speccraft:arch:orchestrate` conductor as a pure `commands/arch/orchestrate.lib.sh` + runbook (shell/markdown, so the TDD guard was ungated and bats supplied RED-before-GREEN, ZERO overrides): token machine `new→reviewed→planned→implemented→validated`, two human-in-the-loop checkpoints (after `planned`, and on review-verdict-escalate) suppressible by `--straight-through`, member dispatch via `(cd '<m>' && /speccraft:spec:<...>)` cwd-scoped (never `--root`), and folded `answer-questions` into `spec:new` so `validate` culminates in `/speccraft:spec:close` — the step that writes the `closed` status reconcile keys `Done` on. 0040 closed the crash window design 0001 named — a phase whose delegated command SUCCEEDED but crashed before the ledger advanced — via `orch_reentry` (adopt iff member-ordinal ≥ phase-completion-ordinal, jumping straight to the artifact's token so an out-of-band `review`+`closed` lands on `validated`), `orch_find_member_spec` (crash-safe `new` adoption keyed on the `informed-by: [design/<id>]` frontmatter `spec:new` actually writes — NOT a `design:` field), and three hermetic crash-leg e2e that PROVE no-double-allocate and no-re-close. 0041 bundled the arc as release 1.12.0. 0042 closed the last gap — `/speccraft:init --workspace` scaffolds a workspace root, writing a `speccraft.toml` with `kind = "workspace"` + a canonical `workspace.yml` (manifest-before-toml write ordering so there's never an orphan `kind = "workspace"`), gated by lazy ledger, `--force` preserves curated manifests, and an explicit `repo → workspace` migration refusal; the model-driven per-candidate approval lives in `commands/init.md` and is fenced by a source-scan + hermetic `workspace_init_cycle.sh`. Two new `speccraft-state` oracles (`config-kind`, `find-workspace-root`) plus release 1.13.0.

### Spec consolidation into domain files
Specs: 0025, 0027, 0028, 0029
Archive: .speccraft/history-archive/history.md
Spec 0025 (2026-06-23) closed the unbounded-growth / signal-decay problem for the spec corpus itself: closing a spec now folds its final requirements into a consolidated, current `specs/domains/<area>.md` instead of leaving N permanent per-feature directories. Merge vocabulary is ADD/MODIFY/REMOVE per requirement modeled on delta-spec; explicit frontmatter `domains:`/`delta:` is authoritative, otherwise a path/title heuristic SEEDS a proposed routing + classification the developer confirms — routing is never silent. Runs INLINE at `/speccraft:spec:close` (step 9), confirm-gated, and NEVER blocks close: decline or open conflict still completes close. Load-bearing pins: every MODIFY/REMOVE carries a REQUIRED verbatim locator matched by exact normalized comparison with the trailing `(spec NNNN)` provenance suffix STRIPPED (0-or->1 match falls through to a non-blocking conflict path, never applied to a guessed line); two clock-free archives — the closed spec DIRECTORY is MOVED wholesale to `specs/.archive/NNNN-slug/` as the LAST step at zero conflicts (LOCATION, not a new `status: consolidated` value, signals "already consolidated" so closed-spec-immutability holds), and superseded requirement TEXT goes to `specs/domains/.archive/<area>.md` under full-entry byte-dedup; per-delta write order is PINNED archive-B-append FIRST → domain mutation → dir-move LAST (both crash windows bats-pinned); `consolidation-conflicts.md` inside the spec dir is the open-conflict sink (deleted on resolution, its absence is the dir-move precondition). `memory-keeper` gains a `# Mode: consolidate` (propose/merge under confirmation) rather than a new agent. Specs 0027/0028 were a three-patch e2e-fixture lineage after 0025 shipped: 0027 (test-harness only) separated decline vs. confirm paths so `[10/13]`'s blanket approval no longer swept dir-relocating consolidation into it — the pre-0025 `exists changelog.md` assertion had failed because the changelog rode along in the wholesale `mv`; 0028 BROKE THE CYCLE by promoting the load-bearing candidate-set/corpus-arrangement logic from a credit-gated `spec_consolidate.sh` fixture into four credit-free `tests/hooks/spec-consolidate.bats` cases (the DECLINE and CONFIRM legs had shared one seeded spec, and a decline writes a PERMANENT skip marker, so decline-then-confirm was self-contradictory) plus a per-leg `consolidate_backfill_candidates` runtime guard so any live seeding drift fails fast and named. 0029 patch-released 1.6.1 with three first-use defects from another project: (A) zsh portability — `${BASH_SOURCE[0]:-$0}` guard across all 8 `*.lib.sh` (bare `${BASH_SOURCE[0]}` aborts under zsh+`set -u`, silently taking down every consolidate function); (B) added a deterministic `consolidate_existing_domains` (live-only, `.archive`-excluded) so routing can prefer good existing domains or deliberately propose new ones; (C) hardened `close.md` step 9 + `memory-keeper` docs so consolidation ROUTES ONLY to `specs/domains/`, never `.speccraft/` (an agent had folded requirements into `.speccraft/architecture.md`/`conventions.md` and called consolidation done). Codified two conventions: cross-shell lib-sourcing idiom + its guard, and "a credit-gated e2e leg must instruct the model to APPLY, not propose-and-wait." No `/speccraft:spec:override` needed across the lineage (all ungated file types); 0027/0028 left the 0025 feature files byte-unchanged.
Supersedes: 0027 → 0028

### Bounded history + reviewable memory-keeper expansion
Specs: 0024
Archive: .speccraft/history-archive/history.md
Made `.speccraft/history.md` bounded instead of unbounded append-only (had reached 22 entries / ~60KB, bloating the `speccraft-context` skill load). Added an explicit, confirm-gated `/speccraft:history:compact`: keep the newest N verbatim (default 10), fold everything older into a merged thematic `## Compacted` section, and move originals VERBATIM into an append-only `.speccraft/history-archive/` folder — double provenance (archive + git), never a deletion. Three design pins from review: (1) the window is POSITIONAL (first N by `## YYYY-MM-DD` in file order, NOT a date sort — the live file is genuinely not date-ordered — and NOT keyed on `(spec NNNN)` because provenance is optional and list-valued); (2) everything is CLOCK-FREE (nudge by entry-count/byte-size, fixed-path archive, byte-match dedup); (3) supersession collapse is restricted to OUT-OF-WINDOW entries with the pointer on the archived side — never mutating a byte-identical window entry — resolving the AC2/AC5 contradiction. `memory-keeper` is REUSED (no new store) via a documented `# Mode: compact` that expands it from append-only to propose/summarize/merge under confirmation — the pattern spec 0025 then generalized. Codified: expose the deterministic seed of a model heuristic at the cheap layer (`history_supersession_seed` pins explicit `supersedes:` + in-body `spec NNNN` xrefs at bats); let the model do only the fuzzy part.

### PM/Architect upstream workflows
Specs: 0022
Archive: .speccraft/history-archive/history.md
Added two optional, advisory upstream workflows — PM (`/speccraft:pm:{new,review,prioritize,close}`) and Architect (`/speccraft:arch:{new,review,decide,close}`) — sitting above the spec lifecycle (PM → Architect → Spec → implement) WITHOUT changing the standalone-specs guarantee: a user who only wants specs+TDD sees zero behavioral change (AC1). PM artifacts under `product/NNNN-slug/`, Architect under `design/NNNN-slug/`; both reuse existing machinery (`cross-reviewer`, `memory-keeper`). Three load-bearing choices: (1) state shape is **additive sibling keys** — `state.go` gains `active_product`/`active_design` as top-level `,omitempty` siblings of `active_spec`, which stays byte-identical on disk; a nested `active.{...}` or `kind`-discriminated record was rejected because it would silently defeat `run.sh`'s `jq -r '.active_spec // "null"'` close-gate, the raw-`jq` revise preflight, and the four e2e fixture literals. (2) AC3 doc-zone is a markdown-scoped regression pin, NOT a `prefix()` entry — `product/`/`design/` `*.md` are always-allowed via the pre-existing `ext==".md"` rule, and `files_test.go` adds positive-markdown rows plus negative-source-under-those-trees rows proving a source file stays TDD-gated; adding those trees to `prefix()` would flip the negatives and reopen the bypass. (3) Cross-stage linkage is pull-only and advisory — `spec:new --from product/<id>|design/<id>` writes a non-empty `informed-by:` key, plain `spec:new` writes nothing (byte-shape parity), and a missing/deleted/`closed` referent NEVER blocks (a `closed` brief is the ideal `--from` source). Closed-artifact immutability stays advisory/by-convention — no status-aware PreToolUse guard, explicitly out of scope. One `/speccraft:spec:override` on the new state-lane fields exposed the guard's Write-tool blind spot (test-file authored via Write → `applyEdit` didn't model `content` → zero red-candidates), filed as a follow-up that became spec 0031.

### Release/version bump pipeline
Specs: 0019, 0021, 0023, 0026
Archive: .speccraft/history-archive/history.md
Version-bump mechanism and the auto-tag → release → self-verify pipeline. 0019 (1.0.0 → 1.1.0) established the coordinated single-spec cut across five live surfaces — the two packaging manifests (`plugin.json`, `marketplace.json`, verified by grep oracle) and the three binary `const version` declarations (`speccraft-{state,guard,drift}`, each pinned RED→GREEN by a sibling `version_test.go` asserting the NEW value so the test fails pre-edit); `-ldflags` injection deferred. 0021 (2026-06-18) closed the "plugin compiles from source at runtime instead of downloading release assets" defect at its root: no `vX.Y.Z` tag had ever been pushed, so tag-triggered `release.yml` never ran, every asset URL 404'd, and `install-binaries.sh`'s silent `&&`/`2>/dev/null` chain fell through to `go build`. Three parts: a new `auto-tag` CI job (push to main, gated) runs `scripts/auto-tag.sh should_tag` and creates+pushes `vX.Y.Z` via `secrets.RELEASE_TAG_PAT` — **NOT** the built-in `GITHUB_TOKEN`, because GitHub intentionally suppresses `on: push: tags` re-triggers for events caused by that token (its infinite-loop guard); a new strong-form `scripts/verify-release.sh` downloads each tarball + `checksums.txt`, recomputes SHA-256, and fails loudly on any missing asset/hash mismatch (wired as `release.yml`'s final step, keyed to `github.ref_name` so it can only run against an existing tag — never a bare `plugin.json` value); `install-binaries.sh` replaces the silent chain with an explicit warn-and-fallback plus a gitignored `.binary-provenance` marker (`download`|`source`) `doctor.sh` reads to report a distinct "built from source" WARN. Producer/consumer asset-name contract fixed: `checksums.txt` (not `checksums-merged.txt`), regenerated over all tarballs post-`merge-multiple`. All four scripts hermetically fixture-tested via `SPECCRAFT_RELEASE_BASE=file://`. Post-close hotfix: `actions/checkout` persists `GITHUB_TOKEN` as an `http.extraheader` that **takes precedence over inline URL userinfo**, so `git remote set-url origin https://x-access-token:${PAT}@…` silently authenticated as the bot — fix is to pass the PAT to checkout itself (`token: ${{ secrets.RELEASE_TAG_PAT }}`), never `git remote set-url`. Codified: `scripts/*.sh` and sibling shell tests are NOT guard-gated (only the four source languages are). 0023 (1.1.0 → 1.5.0) marks the spec-0022 PM/Architect milestone; 0026 (1.5.0 → 1.6.0) marks the README/docs restructure release. Each bump reuses the identical five-surface mechanism, only the value moves.

### TDD guard hardening — red-check correctness, tool-shape blind spots, red-candidate model
Specs: 0009, 0018, 0031, 0032, 0034
Archive: .speccraft/history-archive/history.md
The guard evolved from a touch-check into a real red→green oracle across all four languages, then had its input-modeling blind spots closed. 0009 (2026-06-08) made `/speccraft:spec:override` genuinely single-shot and atomic via a persisted `OverridePending` flag on `Session`, consumed by a new `ConsumeOverride(root) (bool, error)` API that reads-and-clears under a single `mu.Lock()` (the previous mechanism was a no-op for the Go/Python guard); established the "consume-on-use" pattern (`loadStateLocked` → mutate → `saveStateLocked` under one lock) and extended the single-writer allow-list rule to any new `Session` field. 0018 (2026-06-13) closed technical-review finding P0-1: the marketed "red→green invariant" was a true observed-failure check only for Rust; Go/Python/JS-TS merely verified that *a* sibling test file was *touched* this session, so a blank-line touch unlocked every production file in the directory. Generalized spec 0005's Rust-scoped test-runner primitive — new `GoAdapter`/`PytestAdapter`/`JSTSAdapter` (shared for JS+TS, differing only by configured command) reuse `classifyOutcome` via `runner.AdapterForLanguage(lang, cfg)`. The "which test failed" rule mirrors Rust via new capture: `Session.RedCandidates map[string][]string` (JSON `red_candidates,omitempty`, single-writer, cleared on `SessionStart`), populated in the `IsTestFile` dispatch branch by `captureRedCandidates` that diffs pre-edit disk content against the `applyEdit`-modelled post-edit content through per-language regex extractors. Two load-bearing divergences from Rust: (a) the empty just-added set **blocks** for Go/Python/JS-TS because they lack Rust's persisted `rust_test_baseline` — allowing-on-empty would reopen P0-1 via a blank-line touch; (b) an unresolved/uninvocable runner **fails closed** with "no test runner available", never falling back to the touch-check, because a fallback would let an arranged-absent runner reopen the bypass. A 30s `context.WithTimeout` closes the hang vector. AC13 documented the new-symbol bootstrap limitation (a brand-new symbol's just-added test can't compile → `OutcomeBuildFailed` → not a valid RED → one-shot `/speccraft:spec:override`), with an apply-edit-in-memory red-check deferred. 0031 (2026-07-24, v1.7.1) fixed the exact Write-tool blind spot that cost spec 0030 three overrides: `applyEdit` modeled write-tool payload by *shape* (empty `old_string` ⇒ Write ⇒ `new_string` is the file), but the **Write** tool sends `content`, not `new_string`, and `ToolInput` had no `content` field — so a test CREATED with Write extracted zero test IDs, the sibling production edit was blocked with "no failing test observed" despite a real RED on disk, and the fixture (`captureCase`) mis-simulated Write via `NewString` encoding the same wrong assumption as the bug (a green suite over a broken path). Fix discriminates on **tool identity, not payload shape**: added `ToolInput.Content` + a `ToolName` field injected once at `dispatchByLanguage`; `applyEdit` switches on `ti.ToolName` — `Write` → `Content` (incl. empty; `new_string` ignored), `Edit` → in-place replace preserved (never reclassified as Write), `default` (MultiEdit/NotebookEdit/other) → pre-edit unchanged, reserved for spec 0032. Shipped with ZERO overrides via the meta-payoff: every driving RED was authored at the **JSON-envelope boundary** so it compiles against the current struct (no `Content` token) and fails on BEHAVIOR — the technique that lets an additive JSON-decoded surface be extended without the AC13 bootstrap trap. 0032 (2026-07-25, v1.9.0) closed the reserved slot with the same envelope-boundary technique generalized to a struct-FIELD extension (two fields + a named nested type `MultiEditEntry{OldString,NewString}` — named for future referenceability): `applyMultiEdit(pre, edits)` folds each entry as a FIRST-occurrence `strings.Replace` over the RUNNING content (later entries see earlier output), skips `OldString==""` (Go's `strings.Replace` would otherwise prepend), and no-ops on absent target; NotebookEdit → `ti.NewSource` (empty included, mirroring Write). The two 0031 characterization tests were INVERTED + RENAMED (`…CapturesNoRedCandidates` → `…CapturesRedCandidate`) so the NAME is a recurrence signal, plus a `reserved_slot_test.go` recurrence grep forbidding any surviving "reserved spec 0032" language. Zero overrides, confirming the envelope-boundary technique's reach. 0034 (2026-07-25, v1.8.0) made planning/execution stack-agnostic: the EXECUTION substrate was already stack-aware (go/python/js/ts via `runner.AdapterForLanguage`+`cfg.TDD.<Lang>.Command`) but the AUTHORING layer was Go-shaped (`tdd-planner.md` rules 3/7, `plan.md`'s `find … -name '*_test.go'`, `implement.md`/`delegate.md`'s bare `go test ./...`) and the shipped `templates/speccraft/conventions.md` copied Go idioms into every host repo — a direct violation of the standing "templates stay stack-agnostic" guardrail. Fix READS the existing config-backed commands and surfaces them via a new `DetectStack(root, cfg)` probing only exact repo-root manifests with ordered precedence (`manifestOrder`: go > rust > python > ts > js — compiled-lang manifests rank above `package.json`, which is often mere tooling; ts refines js within the package.json case), two new subcommands (`detect-stack` and `test-command`, both versioned/JSON), a single-line `<!-- speccraft:test-command = "cmd" -->` conventions.md marker (per-line, first-of-duplicates wins, empty/malformed → detection fallback, emitted verbatim never shell-evaluated) as the editable source of truth, `/speccraft:init` seeding via `commands/init.lib.sh::seed_conventions` (preserves an existing conventions.md byte-for-byte, writes a TODO on `unknown`), and TWO mechanical meta-guards promoting the template-purity rule from advisory to executable.
Supersedes: 0031 → 0032

### Cross-environment portability & installed-plugin hardening
Specs: 0010, 0011, 0030, 0033
Archive: .speccraft/history-archive/history.md
Making speccraft correct across shells, hosts, and non-dogfood installs. 0010 (2026-06-09) added JS/TS as a first-class language via pure file classification + session-state sibling lookup — 16 suffix variants (`.test`/`.spec` × 8 extensions) plus the `__tests__/` immediate-directory convention, segment-exact `node_modules`/`dist` exclusion, no Node/npm/Jest/Vitest invoked (preserving the "no real runner in the hook" invariant); before adding `jsTsDispatch` the shared red-phase preamble in `goPythonProdGuard` was lifted into a tri-state `prodGuardPrologue` helper returning `prologueAllow`/`prologueBlock`/`prologueContinue`, so adding the next language is a localized `<lang>Dispatch` + `dispatchByLanguage` case + `IsTestFile` extension + fixture + step-counter bump. Four rounds of review pushed back on real-Jest invocation, runtime sibling resolution, and test/prod extension asymmetry — all of which would have broken the existing language model. 0011 (2026-06-09) codified "External-tool boundaries" as a principle: speccraft does not duplicate routing authority for external code-intel tools it does not own — the `speccraft-context` skill, `init` command, and `architecture.md` template no longer name CodeGraphContext as *the* way to answer structural queries; they defer to whatever the user's installed tool has registered (typically via global CLAUDE.md or the MCP server's own instructions). Triggered by a real 2026-06-09 `/spec:new` session where speccraft's skill and the user's global CLAUDE.md gave conflicting routing guidance for the same tool family; the model resolved it in favor of the more specific global rule, but the conflict itself was wasted attention that would silently drift. Established the doc-only spec oracle pattern: a committed `verify.sh` grep-assertion script that fails RED on current main and passes GREEN after the edits — sibling to the E2E language-fixture pattern. Two doc surfaces deferred (README + `speccraft-v1-spec.md`), later cleaned up by spec 0016; `specs/0001-speccraft-v1/spec.md` residual is accepted as historical record under closed-spec immutability. 0030 (2026-07-24, v1.7.0) fixed two coupled command-execution defects surfaced running speccraft as an *installed* plugin (not dogfooded) in another project — macOS default `zsh`, a Python codebase, the natural sequel to spec 0029's first-use-in-another-repo portability lineage: (A) **Plugin-root resolution was unreliable for slash-command bash** — command docs dereferenced `"$CLAUDE_PLUGIN_ROOT/{bin,commands,templates}"`, but `CLAUDE_PLUGIN_ROOT` is only contractually exported to **hook** subprocesses, not to the bash a slash command runs; in the field it resolved to the plugins *parent* directory, so every path failed across most commands. (B) **`revise.lib.sh` was not zsh-safe** — a bare `status` local (zsh reserves `status` read-only, aliasing `$?`) aborted `preflight_status_gate` with `read-only variable: status` when the lib is sourced into zsh. Fix adds `speccraft-state plugin-root` (pure core `ResolvePluginRootFrom(speccraftRoot, claudeRoot, exePath)` + `IsValidPluginRoot` predicate) with precedence `SPECCRAFT_PLUGIN_ROOT` → validated `CLAUDE_PLUGIN_ROOT` (the parent-dir bug fails validation and is skipped, not fatal) → self-derivation (`os.Executable()` → `EvalSymlinks` → ascend to the nearest ancestor holding `.claude-plugin/plugin.json` + `bin/` + `commands/` + `templates/`, handling both `<root>/bin/` and dogfood `<root>/tools/bin/`); 15 command docs migrated to `PLUGIN_ROOT="$(speccraft-state plugin-root)"`; bare `status` renamed `spec_status`. A REAL-zsh `tests/hooks/lib-zsh-safety.bats` (`zsh -uc "source <lib>"` over every `commands/**/*.lib.sh`) is the authoritative backstop for the curated reserved-name set. `.devcontainer` exports `SPECCRAFT_PLUGIN_ROOT="${containerWorkspaceFolder}"` so dogfood sessions never self-derive to the stale cached copy first on `PATH` (a live, verified footgun that led to the "stale-1.1.0-cached-guard" convention recurring across 0031/0032/0034/0035/0036: register decisive REDs via **Edit** not fresh Write, and keep the fresh RED as the LAST sibling test-file touch before a gated prod edit). Cost THREE overrides — root cause was the guard Write-tool blind spot, filed and fixed in 0031. 0033 (2026-07-25) fixed three post-0030/0031 CI regressions test-only (no product code, no version bump, zero overrides — all changed file classes ungated): (1) a macOS-only Go test — `Test_ResolvePluginRoot_SymlinkedExe_ResolvesRealInstall` compared `got` against a raw `filepath.Abs(t.TempDir())`; on macOS `/var`→`/private/var` is a symlink and the resolver correctly `EvalSymlinks`-normalizes the exe, so `got != want` (red on macOS, green on Linux `/tmp` unsymlinked). Fixed with an **asymmetric** assertion: normalize only the EXPECTED value (`want = filepath.EvalSymlinks(root)`), leave `got` untouched — normalizing both sides would let a resolver that STOPPED calling `EvalSymlinks` pass, masking the exact regression the test exists to catch. (2) `rust_integration_cycle.sh` Write payload — the fixture sent `{"tool_name":"Write"}` with content in `new_string` (the exact 0031 mis-simulation, now in a shell fixture). Fixed with `"content"`. (3) `spec_consolidate.sh [cons 1/3]` decline-leg — non-imperative prompt let the credit-gated model propose-and-wait; fixed with an imperative prompt. Added a recurrence guard: `e2e_fixture_shape_test.go` per-envelope scanner (segmenting on `"tool_name"`, associating fields within one block, not a repo-wide proximity grep) extending 0031's Go-ONLY AC5 static guard to SHELL fixtures — the exact gap that let (2) through. Codified two conventions: when a test pins a normalization the product performs (EvalSymlinks, path cleaning), normalize only the EXPECTED value; a Write payload in ANY fixture must carry content in `content`, never `new_string`.

### Revision counter, sanctioned frontmatter writer, and /speccraft:spec:revise
Specs: 0012, 0015, 0035, 0036
Archive: .speccraft/history-archive/history.md
Making `state.json` and spec.md frontmatter writes safe, and adding revise + re-review. 0012 (2026-06-10, v1.1.0 lineage) enforced single-writer rules for files speccraft owns at two layers — the existing source-level grep PLUS a runtime PreToolUse hook check that rejects any `Edit`/`Write`/`MultiEdit`/`NotebookEdit` whose `file_path` canonicalises to `<root>/.speccraft/state.json`. Source-level enforcement alone is insufficient because a `claude -p` lifecycle session can write through a tool call the grep test never sees (a tooling bug had written the literal string `"null"` to `active_spec`, inducing a model workaround that direct-Edited `state.json`). `State.ActiveSpec` gains `,omitempty` (cleared shape on disk is "key absent" rather than a sentinel), new `speccraft-state init` becomes the only sanctioned creation path (idempotent so `/speccraft:init` re-runs cannot nuke session state); `hooks/hooks.json` matchers must be extended in lockstep with a `GATED_TOOLS` enumeration. 0015 (2026-06-11) added `/speccraft:spec:revise` for pre-implementation spec revision — a new `agents/spec-reviser.md` re-runs a Socratic interview while the command body owns all command-only frontmatter mutations (`revision:`, `status:`, `id:`, `created:`); introduced **`commands/spec/revise.lib.sh`** as the first sourceable Bash helper under `commands/spec/`, sourced both by the `.md` body at runtime and by bats at test time — codifying "Sourceable command helpers: `commands/<group>/<name>.lib.sh` colocation" (pure functions only, no top-level side effects). Drift items from the optional `packages[]` cross-check are emitted with the load-bearing `^Q-DRIFT:` prefix anchored at column 0 (structural anchor, not a content guess). Tightened the markdown-frontmatter contract to match the de-facto convention already observed (`agents/` = `name/description/tools/model`; `commands/spec/` = `description/argument-hint/allowed-tools`). Established the mid-implementation amendment convention's second use — T18 reworded AC3/AC4 from "state.json byte-identical" to "`active_spec` field unchanged" because the byte-compare treated normal hook session-tracking as a contract violation. 0035 (2026-07-26, v1.10.0) closed the field finding that a re-run `/speccraft:spec:review` re-litigates already-settled sections every round (a field session took 4 rounds; a hand-built "only assess what changed + check regressions" brief cut it to 2). Added a `--diff` path anchored to a **review-time snapshot** (`specs/<id>/review-snapshot.md`), NOT git history — review→revise→re-review all precede the first commit. New Go: `AtomicWriteFile(path,data,perm)` (same-directory temp + `os.Rename`, injectable `atomicRename` seam for fault-injection); `WriteReviewSnapshot` (RAW-byte sha256, NO CRLF/LF/BOM/trailing-newline normalization, true no-op — no rewrite, no mtime touch — when byte-identical); `ReviewDiff(specDir,promote)` returning versioned `ReviewEnvelope{schema:1,…,fingerprint,base_fingerprint}` with `--promote` freezing the SAME captured bytes AFTER the envelope is computed (read-before-overwrite); `diffSections` a determinism engine keyed per-document by (trimmed heading, 1-based ordinal), matched ordinal-aligned, `modified` emitted only when bodies DIFFER, byte-identical NEVER emitted even under ordinal shift; `WriteReviewFile(path,body,fingerprint)` as a SINGLE atomic commit (strip any prior `reviewed_sha256:` lines, append exactly one canonical line, write the COMPLETE file — never an intermediate fingerprint-free write). Load-bearing correctness pins from 9 rounds of adversarial review: **base_fingerprint provenance** — the envelope exposes the OLD snapshot's sha256 and the command gates BOTH scoped and short-circuit branches on the prior `review.md`'s `reviewed_sha256` equalling it, so a promoted-but-unreviewed baseline (a prior run that promoted then died) forces a FULL review; **retry-safety** — any `changed:false` run lacking a matching review falls back to a full review. 0036 (2026-07-27, v1.11.0) closed the field finding that speccraft had TWO uncoordinated numbering systems and no sanctioned frontmatter writer. The frontmatter `revision: N` counter and the archived `*-r<N>.md` files could drift; the old `preflight_archive_collisions` HARD-REFUSED and `bump_revision`'s hand-rolled `sed -i.bak 's/^status:…/'` ate a newline. Established one **Authority model** — the counter is canonical, archived artifacts are forward-only reconciliation evidence, never a peer authority — with `Effective = hasArchived ? max(fmRev, maxArchived+1) : fmRev`. New declared semantics: within-draft edits keep the same revision (a revision = a completed review cycle / an archive, not a keystroke); the counter advances ONLY via the archive path; `reconcile-revision` is heal-only. New `tools/internal/speccraft/revision.go`: `ComputeRevisionState`; a SINGLE unexported `parseFrontmatterBlock` grammar (BOM-tolerant, per-line CR, column-0 `^key:`, first-wins) is the sole entrypoint BOTH the reader and the byte-safe unexported `setFrontmatterField` writer route through — pinned by a source-scan asserting exactly one `func parseFrontmatterBlock(`. The ONLY exported ops are `SetStatus` (enum-validated) and `SetRevision` (monotonic-forward, REFUSES demotion), both enforcing closed-spec immutability IN the exported op (NO `--force` escape hatch — flagged as a guardrail violation in review and removed). Archive move lives DELIBERATELY in the `speccraft-state` cmd package with `moveArtifactNoReplace` a genuine no-replace primitive (`os.Link` fails EEXIST, unlink source — never racy stat-then-rename) with `os.SameFile` inode-identity recovery on interrupted moves. Meta-guard `frontmatter-writer-guard.bats` is fixture-first with a per-tool matching regime forbidding raw in-place `status:`/`revision:` rewrites in command libs.
Supersedes: 0015 → 0036

### Language support foundations and the runner primitive
Specs: 0002, 0003, 0005, 0007
Archive: .speccraft/history-archive/history.md
Foundational language coverage before JS/TS (0010) and the P0-1 generalization (0018). Specs 0002+0003 (2026-05-15) extended `speccraft-guard`'s red→green detection to Python via a `speccraft.toml` config declaring language, test command, and test-file discovery strategy (sibling vs separate tree), making guard logic language-pluggable through config rather than hard-coded — future languages add a config recipe, not a new binary. 0005 (2026-05-29) added Rust as first-class with three architectural extensions: (1) a shared **test-runner invocation primitive** in `tools/internal/speccraft/runner/` (language-neutral interface + per-language adapters) because Rust's idiomatic unit tests live inline inside `#[cfg(test)] mod tests` blocks within the same `.rs` file, so sibling-edit detection alone cannot distinguish "added a test" from "edited prod" — the runner becomes the authoritative oracle for "did the just-added test actually fail?"; (2) a **dispatch-by-language pattern** in `speccraft-guard` (`dispatchByLanguage` + `rustDispatch`, preserving Go/Python unchanged); (3) a **`reserves-specs` spec-frontmatter field** for forward-referencing follow-up specs by stable ID before they exist on disk (AC #5's workspace-detection error names spec `0006` before it exists). `state.json` gains `rust_test_baseline` (list) + `rust_gate_fingerprint` (string) with the single-writer rule extended to cover both (grep-based regression test). At the time, "retroactive adoption by Go/Python is a non-goal" was written into `architecture.md` — later scrubbed by spec 0018 when the runner was generalized. 0007 (2026-05-29) added the Python e2e fixture (`tests/e2e/python_cycle.sh`, step `[9/9]`) modeled on `rust_inline_cycle.sh`, restoring parity: every supported language (Go, Python, Rust) now has an end-to-end fixture, and future language additions ship with their own `<lang>_cycle.sh` (codified in conventions). Planning was performed with `/speccraft:spec:plan --skip-review` at user direction — the `spec+plan` was paired for PR review; future readers should know the normal review gate did not run.
Supersedes: 0005 → 0018

### E2E harness discipline: model-behavior isolation, structural predicates, and CI job split
Specs: 0008, 0013, 0014, 0017, 0020
Archive: .speccraft/history-archive/history.md
Making the e2e lifecycle a signal, not a flake. 0008 (2026-05-29) split the e2e workflow into two CI jobs with different cost/credential profiles: `e2e-language-only` (cheap, hermetic, no `ANTHROPIC_API_KEY`, runs on every push and PR) executes the language-dispatch fixtures via `tests/e2e/run.sh --language-only`; `e2e-devcontainer` (expensive, requires API credits, gated to `push` on `main`) continues to run the full `claude -p`-driven lifecycle. Layered in an `ENVIRONMENT_FAILURE:` annotation model so lifecycle-job logs distinguish environmental issues (`credit_exhausted`, `auth`, `transient_api`) from real assertion failures — the canonical pattern for env-failure observability. Defensive idempotent ownership fix for `~/.claude/session-env` in `.devcontainer/setup.sh`. Recorded the pre-close gate (first green `e2e-language-only` run on `main`) verbatim in the spec's changelog as the first concrete enforcement of the §Post-merge verification convention. Integration surfaced a latent mock-stdin bug: `claude -p`-launched subagent CLIs never EOF child stdin, so mocks doing `INPUT="$(cat)"` block forever — fix `exec </dev/null` at the top of every mock aux-agent script (convention for any future mock CLI invoked through the aux-delegator path). 0013 (2026-06-10) completed the post-0012 dead-code cleanup (the two defensive `ActiveSpec == "null"` reads at `root.go:45` and `guard/main.go:353`) under sibling-test pins, and codified the **mid-implementation amendment** convention in `.speccraft/conventions.md` § Spec lifecycle: when CI surfaces a bounded issue between push and close that shares the active spec's theme and (a) is a strictly one-file edit, (b) keeps main CI red until it lands, and (c) does not require any AC change other than additive, the issue MAY be folded into the active spec as a new task + new AC + a dated `## Amendment` section, rather than filed as a follow-up. Also formalised the close-gate-pending workaround (a `<!-- TODO: <github-actions-run-url> -->` marker in the changelog, satisfied by a subsequent spec's CI run with explicit cross-reference; the predecessor's TODO is left in place per closed-spec immutability). T6 added a Go setup + helper-binary build step to the CI `hooks:` job — spec 0012's new bats reject cases were silently no-op'ing because `speccraft-state` was not on `$PATH` in CI. 0014 (2026-06-10) codified: when an e2e assertion verifies that a model-driven step happened (memory-keeper applied an ADR, spec-author wrote a plan, planner emitted a `## Risk` section), the predicate must target a STRUCTURAL signal the agent's contract guarantees, not a CONTENT signal the agent's free-text choice happens to produce — `[7/9] /speccraft:spec:close` now matches the dated ADR-header shape `^## 20[0-9]{2}-[0-9]{2}-[0-9]{2}` (via new `contains_regex`, `grep -qE`) rather than the literal word `farewell`. Helpers extracted to `tests/e2e/lib.sh` sourced by both `run.sh` and a new `contains_adr_assertion_test.sh` fixture, so the predicate is **provably identical** between production harness and fixture ("naive `source run.sh` from a fixture executes the entire harness; helper duplication invites silent drift"). New `run_helper_unit_tests()` runs first in both `--language-only` and full-lifecycle paths — helper regressions fail fast. Codified as two conventions: "E2E assertion predicates: structural over content" and "Shared assertion helpers via tests/e2e/lib.sh". 0017 (2026-06-12) pinned the e2e harness model explicitly — `run_claude()` now passes `--model "${CLAUDE_MODEL:-claude-opus-4-8}"`, so every `claude -p` call selects an explicit, overridable model rather than silently inheriting whatever the account/CLI default happened to be. The spec was reviewed with `claude-sonnet-4-6` as the cost-optimization default; a same-cycle amendment reverted to `claude-opus-4-8` after the validation-gate `e2e-devcontainer` run failed at `[9/13] TDD invariant` with no `ENVIRONMENT_FAILURE` tag (on Sonnet the model invoked `/speccraft:spec:override` on the GREEN step and stalled). The Sonnet failure is a concrete instance of the spec-0014 "structural over content" lesson generalized: the e2e lifecycle's *behaviour* (not just its assertion phrasing) varies by model. Third use of the mid-implementation amendment pattern. 0020 (2026-06-15) applied the "meta-test reads run.sh's live predicate" pattern its second use: swapped the `[6/13] revise no-op` fixed-string grep to `contains_regex "[Nn]o.?op|[Nn]o changes|byte-identical|unchanged"` (marker is deterministic; the model's surrounding final-message paraphrase is not) and added `tests/e2e/revise_noop_assertion_test.sh` mirroring the 0014 pattern — it reads run.sh's *live* assertion line and pattern at runtime so the two cannot silently diverge, wired into `run_helper_unit_tests()` and thus a real close gate that needs no API credits.

### Doc-only scrubs, plugin packaging, and initial adoption
Specs: 0001, 0016 — and un-suffixed maintenance entries
Archive: .speccraft/history-archive/history.md
Ambient/historical decisions: **speccraft adopted** (2026-05-28, spec 0001) — adopted for spec-first TDD workflow; all future code changes go through `/speccraft:spec:new`. **Plugin packaged via `dcstolf-tools` marketplace** (2026-05-10, no spec) — shipped as a single-plugin entry inside the marketplace (`.claude-plugin/plugin.json` + root `marketplace.json`); install path is now load-bearing (do not introduce a second entrypoint), and `marketplace.json` must validate against the upstream JSON Schema. **Slash-command names fully qualified to `/speccraft:spec:*`** (2026-05-22, maintenance, no spec) — migrated all slash commands from bare names (`/spec:new`) to the fully qualified plugin form in README, e2e tests, and every command file's "next steps" hints; bare names collide with host-repo commands once installed via marketplace. **Retroactively closed the genesis spec 0001** (2026-07-29) — status reconciliation only, no code change: the project-genesis spec (bootstrapped the plugin: hooks, three Go binaries, commands, subagents, `.speccraft/` memory, e2e harness) had sat at `in-progress` since 2026-05-09 with 58/69 tasks done — the 11 open ones all manual-verification/early-phase-e2e placeholders long since superseded by the shipped `tests/e2e/run.sh` harness, the release/verify pipeline (spec 0021), and the mock-agent e2e. Closed to reflect reality (v1 shipped and was hardened by the 40 specs through 0040). **Spec 0016** (2026-06-11) closed spec 0011's deferred doc-only scrub of the two human-facing prose surfaces (`README.md` at 3 sites + `speccraft-v1-spec.md` at 5 sites): eight prescriptive routing phrases ("prefer X", "should install X", "X is the recommended way") replaced with neutral factual descriptions and example framing ("such as CodeGraphContext"), preserving the neutral anchors `Recommended companions` (README) and `**Recommended companion:**` (v1-spec §13 bolded label). A new `verify.sh` (12 labelled `grep -F` checks: 5+1 README, 5+1 v1-spec) is the AC oracle — every check is file-scoped by name because the absence-target strings literally appear inside this spec's own `spec.md`, and repo-wide `grep -r` is forbidden. A defensive paraphrase pin — trivially green this cycle — exists to fail RED if a future rewrite reintroduces a near-variant of the banned wording. Second use of the doc-only-spec grep-oracle pattern codified by 0011; label presence checks as explicitly as absence checks so failure messages distinguish over-deletion from missed scrub. AC4 closed-spec immutability held (`git diff cf0d094..HEAD -- specs/0001-speccraft-v1/spec.md` empty).
